# 第四章使用说明：基于RL的模板调度方法（POP-T）——训练与可视化流程

> 说明：训练入口基于 `scripts/train/train_jsbsim.py`，渲染入口基于 `scripts/render/render_jsbsim.py`。
> `chapters/ch04_popt/` 目录提供“论文复现入口脚本”，对 1v1 与 2v2 流程做了固定映射与参数最小化封装。

## 章节目标
复现并验证：
- 1v1 ShootMissile（自博弈/层级控制）训练流程可运行；
- 2v2 ShootMissile（共享/多智能体）训练流程可运行；
- 训练产物（actor/critic）可用于渲染生成 TacView 轨迹。

## 工程入口
- 1v1 训练入口：`train_1v1_shoot_selfplay.py`
- 2v2 训练入口：`train_2v2_shoot_selfplay.py`
- 1v1 渲染入口：`render_1v1.py`
- 2v2 渲染入口：`render_2v2.py`

## 训练脚本映射（1v1 vs 2v2）
| 任务 | env-name | scenario-name（对应yaml） | algorithm-name |
|---|---|---|---|
| 1v1 ShootMissile 自博弈 | `SingleCombat` | `1v1/ShootMissile/HierarchySelfplay` | `ppo` |
| 2v2 ShootMissile 自博弈/共享 | `MultipleCombat` | `2v2/ShootMissile/HierarchySelfplay` | `mappo` |

## 操作步骤
### A. 1v1 训练
```bash
python chapters/ch04/train_1v1_shoot_selfplay.py --seed 1 --cuda 0
```

### B. 2v2 训练
```bash
python chapters/ch04/train_2v2_shoot_selfplay.py --seed 0 --cuda 0
```

### C. 渲染（生成 .acmi）
1) 找到训练输出目录（见“结果产物”），选定一个 `run*` 文件夹作为 `--model-dir`；
2) 执行渲染：
```bash
python chapters/ch04/render_1v1.py --model-dir scripts/results/SingleCombat/1v1/ShootMissile/HierarchySelfplay/ppo/ch04_popt_1v1_shoot/run1
python chapters/ch04/render_2v2.py --model-dir scripts/results/MultipleCombat/2v2/ShootMissile/HierarchySelfplay/mappo/ch04_popt_2v2_shoot/run1
```

## 关键参数（仅保留必须关注的）
- `--seed`：随机种子
- `--cuda`：是否使用 GPU（1=使用，0=不用）
- `--num-env-steps`：总训练步数（默认给出较小值便于自检，正式复现可调大）
- `--n-rollout-threads`：并行环境数量（越大采样越快，但更耗资源）

## 结果产物
训练输出目录固定为：
- `scripts/results/<env-name>/<scenario-name>/<algorithm-name>/<experiment-name>/run*/`
其中包含 `actor_latest.pt` / `critic_latest.pt` 等文件。

渲染输出目录为：
- `scripts/results/<...>/<experiment-name>/render/`
其中包含 `*.acmi` 轨迹文件。

## 验收标准
- 训练脚本能够开始采样与更新（终端持续输出训练信息，且目录生成 `run*`）
- 渲染脚本能输出 `.acmi` 并能被 TacView 打开

## 注意事项
- 首次训练建议先用较小 `--num-env-steps` 做“烟雾测试”，确认能跑再调大。
- 2v2 更耗算力/内存，资源不足可减小 `--n-rollout-threads`。
