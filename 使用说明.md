# 第五章使用说明：协同训练（CC-IVB/CC-SVB）与对比复核

## 章节目标
复现并验证协同训练入口可运行，并支持与第四章（无协同项）进行对比：
- 开启 CC-IVB/CC-SVB（团队 critic）与 CCC 协同奖励塑形；
- 关闭 CC-IVB/CC-SVB 与 CCC 协同奖励塑形，作为对照组；
- 通过训练日志中的协同成功率/关键指标进行结果复核。

## 工程入口
- 开启协同：`train_ccivb.py`
- 关闭协同：`train_baseline.py`
- 日志对比：`compare_logs.py`

底层训练脚本：
- `scripts/train/train_hybrid_red.py`

关键开关（运行时环境变量）：
- `USE_CC_SVB`：1=开启 CC-IVB/CC-SVB 团队 critic；0=关闭
- `USE_CCC_RSHAPE`：1=开启 CCC 协同奖励塑形；0=关闭
- `EXP_NAME`：实验名（用于区分输出目录）

## 操作步骤
### A. 开启协同训练（CC+CCC）
```bash
python chapters/ch05/train_ccivb.py
```

### B. 关闭协同训练（对照组）
```bash
python chapters/ch05/train_baseline.py
```

### C. 结果复核（对比两次训练日志）
```bash
python chapters/ch05/compare_logs.py --log-a scripts/results/MultipleCombat/2v2/ShootMissile/HybridRL/ch05_ccivb/train.log --log-b scripts/results/MultipleCombat/2v2/ShootMissile/HybridRL/ch05_baseline/train.log
```

## 关键参数
- 训练迭代次数、保存频率等超参在 `scripts/train/train_hybrid_red.py` 顶部配置区。
- 环境配置固定为 `envs/JSBSim/configs/2v2/ShootMissile/HierarchySelfplay.yaml`

## 结果产物
输出目录固定为：
- `scripts/results/MultipleCombat/2v2/ShootMissile/HybridRL/<EXP_NAME>/run*/`
包含：
- `actor_latest.pt` / `critic_latest.pt`

日志落盘：
- `scripts/results/MultipleCombat/2v2/ShootMissile/HybridRL/<EXP_NAME>/train.log`

## 验收标准
- 训练开始后终端持续输出迭代信息；
- 若开启 CCC，会周期性输出 `[CCC] ... rate=...`；
- 保存触发后生成 `run*/actor_latest.pt` 等文件。

## 注意事项
- 若希望只开 CC-IVB 不加 CCC：设置 `USE_CCC_RSHAPE=0`（入口脚本里已区分）。
