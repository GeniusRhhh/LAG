第三章  基于“不确定性传播-协同闭环”七层联动的协同探测决策方法

3.1引言

现代空战已进入超视距作战时代，战场态势感知能力成为制胜关键。预警机凭借其远距离探测能力可提供早期预警，但探测精度较低；战斗机火控雷达探测距离有限，但可提供高精度目标定位。因此，如何有效融合这两类异构传感器信息，实现从预警机粗略探测到火控雷达精确跟踪的快速转换，是当前亟待解决的关键技术问题。本章针对该问题建立数学模型，并在此基础上给出协同探测决策与雷达扫描分配算法。

3.1.1 协同探测决策问题定义

考虑由 N 架战斗机组成的编队执行空中巡逻（Combat Air Patrol, CAP）任务（本文取 N=4 的双机对编队形），编队依托预警机与战斗机火控雷达两类信息源（预警机提供远程目标信息，战斗机火控雷达为机载传感器）编队配置与战斗机火控雷达两类传感器。预警机具有远距离探测能力（探测距离记为 R_awacs），但位置精度较低（位置误差记为 σ_awacs），更新周期为 T_awacs；火控雷达探测距离较近（探测距离记为 R_radar，本文以进入该门限作为“到位”判据），但位置精度高（位置误差记为 σ_radar），扫描周期可变（由第3.5节体制参数确定）。两类传感器精度关系满足：

σ_awacs≫σ_radar

Ω_awacs≫Ω_radar

其中 Ω_awacs=360° 为预警机视场角（全向），Ω_radar∈{20°,60°,120°} 为火控雷达方位扫描视场角（对应NARROW/MEDIUM/WIDE三档扫描模式；在3.5节中亦采用等价的“方位半宽”表述 θ_range∈{10^∘,30^∘,60^∘}，满足 Ω_radar=2θ_range）。

在动态对抗环境下，从预警机探测到目标到火控雷达建立稳定航迹这一阶段，如何规划编队的引导路径（航向、速度、编队协调），实现最短时间引导编队到达敌方大致位置，为后续雷达精确探测创造条件。

本文研究的协同探测决策作用范围覆盖“预警发现—编队引导—雷达截获—稳定保持”全过程：从预警机首次探测到目标，到目标进入火控雷达探测范围（距离门限 R_radar），再到进入火控雷达探测范围后的稳定保持阶段。主关注为 0–R_radar 阶段的“预警引导协同探测”（航向/速度/编队协调）；进入 R_radar 门限后，本文给出与该阶段衔接的雷达扫描模式与扫描分配策略（第3.5节），用于“到位后的截获与稳定跟踪”所需的资源调度，但不讨论武器发射与战术机动规划，相关环节通过高精度航迹接口与后续模块解耦。

3.1.2任务场景约束

重要约束（初始态势）：在当前任务场景中，敌方初始航向固定为180°（由北向南的对头接近初态），但从仿真开始后敌方可根据机动策略在任意时刻改变航向与速度（如转向、上高下高等），因此敌方航向是时变且不确定的。该约束意味着：算法设计不应依赖“来袭方向恒定”的假设，而应以机动不确定性传播为核心，通过对敌方未来可达域/置信域的滚动估计实现引导。

我方初始状态：编队配置：左编队（A0100长机+A0200僚机），右编队（A0300长机+A0400僚机）；初始航向：长机0°（朝北），僚机180°（朝南）；初始位置：在算法设计中视为给定（可为任意满足任务约束的配置）；巡逻状态：热段（长机，雷达开机）与冷段（僚机，雷达关机）交替。

敌方初始状态：初始航向：180°（由北向南的对头接近初态），但航向可随时间变化（机动不确定）；初始距离：300-400km（典型值300km）；编队空间分布：可能为密集排列、标准间距、分散排列、纵队排列、宽正面排列等任意一种；高度分布：可能为高高度、中高度、低高度、混合高度、阶梯高度、突现高度中的任意一种；速度配置：可能为正常速度、低速、高速、加速、混合速度中的任意一种。

图3.1 CAP任务场景示意图

<font color="red">上图展示了CAP任务的典型初始态势：我方4机编队采用冷热交替巡逻（长机雷达开机朝北，僚机雷达关机朝南），敌方编队由北向南接近（初始航向180°），初始距离300-400km。</font>

3.2系统状态空间模型

3.2.1目标运动模型

为描述目标的机动特性，本文采用交互多模型（Interacting Multiple Model, IMM）方法，包含四个子模型：匀速直线运动（Constant Velocity, CV）、协调转弯（Coordinated Turn, CT）、匀加速运动（Constant Acceleration, CA）和Singer随机机动模型。IMM方法通过多个运动模型的并行运行和概率加权融合，能够自适应地估计目标的多种机动模式，从而有效处理敌方机动不确定性。

(1) 匀速直线运动模型（CV）

CV模型假设目标在二维平面内以恒定速度做直线运动，适用于目标保持航向不变的情况。目标状态向量定义为 x_k=[x,x ̇,y,y ̇]^T，其中 (xⓜ,y) 为目标在二维平面内的位置坐标，(x ̇ⓜ,y ̇ ) 为目标的速度分量。CV模型的状态方程为：

x_(k+1)=F_CV x_k+G_CV w_k 									(3-1)

其中状态转移矩阵 F_CV 描述了位置和速度在时间步长 Δt 内的线性演化关系：位置分量等于上一时刻位置加上速度乘以时间步长，速度分量保持不变。过程噪声驱动矩阵 G_CV 将过程噪声 w_k 映射到状态空间，具体形式为：

F_CV=[■(1&Δt&0&0@0&1&0&0@0&0&1&Δt@0&0&0&1)],G_CV=[■((Δt^2)/2&0@Δt&0@0&(Δt^2)/2@0&Δt)] 					(3-2)

过程噪声 w_k∼N(0,Q_CV) 为零均值高斯白噪声，用于建模目标运动中的随机扰动。协方差矩阵 Q_CV=q_CV⋅I，其中 q_CV 为过程噪声强度，I 为单位矩阵。

(2) 协调转弯模型（CT）

CT模型考虑目标以恒定角速度 ω 进行协调转弯机动，适用于目标执行转弯、盘旋等机动动作的情况。状态向量扩展为 x_k=[x,x ̇,y,y ̇,ω]^T（5维），增加了角速度 ω 作为状态分量。状态方程为：

x_(k+1)=F_CT (ω)x_k+G_CT w_k 								(3-3)

其中 F_CT (ω) 为依赖于转弯角速度的非线性状态转移矩阵，其形式考虑了转弯过程中速度方向的连续变化。G_CT为过程噪声驱动矩阵，将过程噪声w_k映射到状态空间（与 CV 类似，具体形式取决于离散化与角速度维的噪声假设）。当角速度 ω 接近零时，CT模型退化为CV模型。

(3) 匀加速运动模型（CA）

CA模型考虑目标加速度变化的情况，适用于目标执行加速、减速等机动动作。状态向量扩展为 x_k=[x,x ̇,x ̈,y,y ̇,y ̈]^T（6维），增加了加速度分量 (x ̈ⓜ,y ̈ )。状态方程为：

x_(k+1)=F_CA x_k+G_CA w_k 									(3-4)

其中状态转移矩阵 F_CA 描述了位置、速度和加速度之间的积分关系：速度等于上一时刻速度加上加速度乘以时间步长，位置等于上一时刻位置加上速度乘以时间步长再加上加速度乘以时间步长的平方的一半。G_CA为过程噪声驱动矩阵，将过程噪声映射到状态空间中。

(4) Singer随机机动模型

Singer随机机动模型用于描述目标加速度随时间连续变化、但又带有随机扰动的机动过程。其基本假设是：目标加速度不是恒定值，而是满足一阶时间相关的随机过程，即当前时刻加速度与上一时刻加速度相关，并在随机激励作用下能够逐步变化。与CA模型直接把加速度视为确定状态不同，Singer模型更强调机动保持性与随机性并存，适用于敌方执行不规则转弯、间歇加减速或难以事先确定规律的复杂机动情形。

 在二维平面内，Singer模型可将状态向量写为 (x_k=[x,x ̇,a_x,y,y ̇,a_y ]^T)。其核心假设为加速度满足一阶时间相关过程a ̇(t)=-αa(t)+w(t)其中 α=1/τ为机动时间常数的倒数，τ反映机动保持时间，w(t)为随机激励项。离散化后可写为a_(k+1)=e^(-αΔt) a_k+η_k其中η_k为离散过程噪声。该模型表明目标加速度具有“逐步衰减 + 随机扰动驱动”的特征，因此适合描述持续但不规则的随机机动过程。

(5) 模型转移概率

IMM方法通过马尔可夫链描述不同运动模型之间的转移关系。四个模型之间的转移概率由马尔可夫转移概率矩阵 Π 描述：

Π=[■(π_11&π_12&π_13&π_14@π_21&π_22&π_23&π_24@π_31&π_32&π_33&π_34@π_41&π_42&π_43&π_44 )] 							(3-5)

其中 π_ij 表示从模型 i 转移到模型 j 的概率，满足概率归一化条件 ∑_(j=1)^4▒π_ij =1。该转移矩阵反映了目标在不同机动模式之间切换的可能性，对角线元素 π_ii 通常较大，表示目标倾向于保持当前机动模式。

3.2.2传感器观测模型

传感器观测模型描述了预警机和火控雷达对目标状态的测量过程，将目标在笛卡尔坐标系中的状态（位置、速度等）转换为传感器坐标系中的观测值（距离、方位角等），并考虑测量误差的影响。

(1) 预警机观测模型

预警机作为远程探测平台，能够提供目标相对于预警机位置的极坐标观测值，包括距离和方位角。观测方程为：

z_k^awacs=h_awacs (x_k)+v_k^awacs								 (3-6)

其中观测函数 h_awacs (x) 将目标在笛卡尔坐标系中的位置 (xⓜ,y) 转换为极坐标系中的距离和方位角：

h_awacs (x)=[█(√(x^2+y^2 )@atan2(x,y))] 								(3-7)

观测函数的第一行计算目标到预警机的欧氏距离，第二行计算目标相对于预警机的方位角（以正北方向为0°，顺时针为正）。观测噪声 v_k^awacs∼N(0,R_awacs) 为零均值高斯白噪声，其协方差矩阵为 R_awacs=diag⁡(σ_awacs^2,σ_(θ,awacs)^2)，其中 σ_awacs 为距离测量精度（标准差），σ_(θ,awacs) 为方位角测量精度（标准差）；非对角元素为0，表示距离与方位角测量误差相互独立。

(2) 火控雷达观测模型

第 i 架战机的火控雷达观测模型与预警机类似，但观测值是相对于战机自身位置的。观测方程为：

z_k^(radar,i)=h_radar (x_k,x_i)+v_k^radar 							(3-8)

其中 x_i=[x_i,y_i ]^T 为第 i 架战机在笛卡尔坐标系中的位置，观测函数为：

h_radar (x,x_i)=[█(√((x-x_i )^2+(y-y_i )^2 )@atan2(x-x_i,y-y_i))] 					(3-9)

观测函数的第一行计算目标到第 i 架战机的相对距离，第二行计算目标相对于战机的方位角。观测噪声 v_k^radar∼N(0,R_radar) 为零均值高斯白噪声，其协方差矩阵为 R_radar=diag⁡(σ_radar^2,σ_(θ,radar)^2)，其中 σ_radar、σ_(θ,radar) 分别为距离与方位角测量精度；

精度关系：两类传感器满足 σ_awacs≫σ_radar、σ_(θ,awacs)≫σ_(θ,radar)，火控雷达测量精度高于预警机，预警机探测距离更远，两者形成互补关系。

3.2.3 IMM-EKF状态估计算法

本节状态估计的目的，而在于为后续扫描区间决策提供可用的目标状态输入。预警机给出的目标位置存在误差，且目标可能发生转弯、加减速或随机机动；若直接依据瞬时观测结果安排扫描中心与扫描范围，容易出现扫描区间偏小或扫描中心偏移。因此，本文采用 IMM-EKF 对目标当前位置、速度及其不确定性进行联合估计，并输出协方差 P，作为第3.4节机动不确定性传播、最小有效扫描区间构造和后续滤波递推的基础输入。

其中，EKF（Extended Kalman Filter，扩展卡尔曼滤波）是面向非线性系统状态估计的递推方法。其基本过程包括两步：首先依据运动模型对目标下一时刻状态进行预测；随后利用当前量测对预测结果进行修正。由于本章的观测方程h(⋅) 以及部分机动模型含有非线性项，因此需在当前估计点附近进行一阶线性化，再按卡尔曼滤波形式完成更新。

IMM-EKF 用于解决“目标可能在多种机动模型之间切换”的问题。其计算流程为：先按模型转移概率对上一时刻各模型状态进行混合；再在 CV、CT、CA、Singer 四个模型下分别执行一次 EKF 预测与修正；随后利用量测残差似然更新各模型概率；最后按模型概率加权融合，得到当前时刻的统一状态估计与协方差。该协方差将直接传递给第3.4.3节，用于构造预警失效或目标机动增强条件下的最小有效扫描区间。

设共有 M_imm=4 个机动模型（CV/CT/CA/Singer），模型转移矩阵为 Π=[π_ij]，第 m 个模型在时刻 k 的状态与协方差为 (x ̂_k^((m) )ⓜ,P_k^((m) ) )，模型概率为 μ_k^((m) )。

算法3.1 IMM-EKF状态估计（单目标）

```text
输入：上一时刻各模型状态/协方差/概率 {(x ̂_(k-1)^((m) ),P_(k-1)^((m) ),μ_(k-1)^((m) ))}，转移矩阵 Π，当前观测 z_k（预警机或雷达），过程噪声 {Q^((m) ) }，观测噪声 R。
输出：融合状态 x ̂_k，融合协方差 P_k，各模型后验 {(x ̂_k^((m) ),P_k^((m) ),μ_k^((m) ))}。
1   for j ← 1 to M_imm do // ① 模型混合：对每个模型 j，计算其混合初值（状态与协方差）
2       c_bar (j)  ← Σ_i π_ij (i)_μ^(k-1) // 混合权重归一化常数：所有模型转移到模型j的概率加权和
3       for i ← 1 to M_imm do (i∣j)_μ^(k-1) ← π_ij (i)_μ^(k-1) / c_bar (j) // 混合概率：模型 i在给定模型j下的条件概率
4       x_(0,j) ← Σ_i (i∣j)_μ^(k-1) (i)_x ̂^(k-1) // 混合状态：按混合概率加权融合各模型上一时刻状态，作为模型 j的预测初值
5       P_(0,j) ← Σ_i (i∣j)_μ^(k-1)[ (i)_P^(k-1) + ((i)_x ̂^(k-1)−x_(0,j)) 〖(·)〗^T ] // 混合协方差：含各模型协方差与状态误差外积项，保证协方差一致性
6   for j ← 1 to M_imm do // ② 各模型 EKF 预测与量测更新：对每个模型独立执行 EKF 一步
7       x_(pred,j) ← f^((j) )(x_(0,j)); P_(pred,j) ← F^((j) ) P_(0,j) F^((j)T) + Q^((j) ) // EKF 预测：用模型的非线性/线性状态转移函数预测状态与协方差
8       K_j ← P_(pred,j) 〖H^T  (H P_(pred,j)  H^T  + R)〗^(-1)
9       (j)_x ̂^k ← x_(pred,j) + K_j (z_k − h(x_(pred,j))); (j)_P^k ← (I − K_j H) P_(pred,j) // 状态更新：用创新（量测残差）修正预测状态；协方差更新：量测后不确定性减小
10      ν_j ← z_k − h(x_(pred,j)); S_j ← H P_(pred,j) H^T + R
11      Λ_j ← N(ν_j; 0, S_j); (j)_μ^k ← Λ_j c_bar (j) / Σ_l Λ_l c_bar (l)
12  x ̂_k ← Σ_j (j)_μ^k (j)_x ̂^k // ④ 融合状态：按更新后的模型概率加权融合各模型状态，得到最终估计
13  P_k ← Σ_j (j)_μ^k[ (j)_P^k + ((j)_x ̂^k−x ̂_k) 〖(·)〗^T ]
14  return (x ̂_k, P_k, {(j)_x ̂^kⓜ,(j)_P^kⓜ,(j)_μ^k }) // 返回融合状态、融合协方差，以及各模型的后验状态、协方差与概率（供下一时刻使用）
```

式中 〖(·)〗^T 表示前一项误差向量与其转置的外积，即 P_(k-1)^((i) )+(x ̂_(k-1)^((i) )-x ̂_(k-1)^(0(j)))(x ̂_(k-1)^((i) )-x ̂_(k-1)^(0(j)) )^T 与融合时的类似项。

 从决策用途看，IMM-EKF 每处理一次新量测，输出的不是单一点坐标，而是“目标当前位置 + 位置不确定性范围”。当预警机信息连续可用时，该结果用于缩小定向扫描责任区；当预警机信息失效或目标连续机动时，该结果用于外推目标可能位置，并据此重新组织多机搜索责任。

在单目标、单次量测下，对多模型（CV/CT/CA/Singer）做一次完整的 IMM 步：先按转移概率混合各模型上一时刻的状态与协方差得到各模型的混合初值；再对每个模型做 EKF预测与更新，并用当前量测的创新似然更新模型概率；最后按模型概率加权融合得到当前时刻的融合状态与协方差，供第3.4.2节置信域预测与滤波递推使用。

3.3优化目标与约束条件

3.3.1优化目标函数

协同探测阶段的核心任务，不是单纯优化编队飞行过程，而是在给定雷达体制与多机可用性约束下，以尽可能短的时间完成对全部目标的发现与持续保持，并在预警失效或目标机动增强时，动态构造覆盖目标可能位置的最小有效扫描区间。因此，本节将协同探测决策目标概括为“最快发现、最小有效扫描、有限资源分配”三项内容。

具体而言，协同探测决策需要同时回答以下三个问题：其一，在预警信息有效时，如何根据目标方位集合和我方可用飞机状态，尽快将扫描责任分配到各机并形成对全部目标的有效覆盖；其二，在预警信息失效或目标机动导致空间预测困难时，如何利用状态估计与无量测预测结果构造最小有效扫描区间，避免扫描范围不足；其三，在机载雷达资源受限条件下，如何兼顾目标发现速度与持续跟踪保持。

在当前实现中，协同探测任务分配分为两类情况：当预警信息有效时，系统依据目标方位集合、敌方中心估计结果和我方飞机当前朝向关系，优先形成“4个目标责任方向—4架飞机”的首轮责任覆盖；若几何条件允许，则各机分别对一个目标方向承担主责，若某一时刻仅有少数迎敌向飞机具备直接火控条件，则先由其承担前出搜索责任，其余飞机在转向到位后接续对应责任；当预警信息失效或目标连续机动导致空间预测困难时，系统转而依据 IMM-EKF 预测结果和置信区域，将搜索责任优先分配给当前具备有效火控雷达几何条件的飞机。考虑当前 CAP 双机对、冷热交替巡逻和迎敌几何关系，初始阶段通常仅有少数迎敌向飞机承担主搜索责任，其余飞机在转向到位后再接入协同覆盖。

协同探测决策的总体目标是在满足约束条件下，最小化加权代价函数：

(min)┬{θ_i (t),v_i (t)}  J=α_1⋅T_max+α_2⋅C_resource+α_3⋅P_miss 	(3-10)

其中：T_max=〖max⁡〗_i T_i：占位时间，完成全目标有效发现或建立期望跟踪状态所需的总体时间；     后

C_resource：资源代价，表示扫描扇区宽度、扫描周期、可用雷达数量以及多机扫描责任展开带来的资源占用强度。该项主要体现在扫描扇区宽度、扫描周期以及多机扫描中心分布三个方面：扇区越宽、扫描周期越长，或多机扫描中心展开越大，说明系统为扩大覆盖范围投入的探测资源越多；

P_miss：目标丢失概率，表示目标脱离期望扫描覆盖或稳定跟踪状态的风险；

{θ_i (t),v_i (t)}：决策变量集合，包括每架战机的航向序列和速度序列；

α_1,α_2,α_3：权重系数，反映不同目标的相对重要性；

到达时间定义：第 i 架战机到达目标区域的时间 T_i 定义为：

T_i=min{t:||p_i (t)-p ̂_enemy ||≤R_target} 							(3-11)

其中 p ̂_enemy 为估计的敌方编队中心，R_target 为目标区域半径（考虑预警机误差和敌方分布范围）。

3.3.2约束条件

协同探测决策优化问题需满足以下约束条件：

(1) 速度约束

每架战机的速度需在允许范围内：

v_min≤v_i (t)≤v_max,∀i,∀t∈[0,T_i] 									(3-12)

其中 v_min 和 v_max 为战机的最小和最大速度限制。

(2) 航向变化率约束

航向变化需满足物理限制：
|θ_i (t+Δt)-θ_i (t)|≤Δθ_max⋅Δt,∀i,∀t 					(3-13)

其中 Δθ_max 为最大航向变化率（度/秒），Δt 为时间步长。

(3) 加速度约束

速度变化需满足加速度限制：
|v_i (t+Δt)-v_i (t)|≤a_max⋅Δt,∀i,∀t 				(3-14)

其中 a_max 为最大加速度限制。

(4) 编队安全距离约束

编队内战机需保持安全距离：
||p_i (t)-p_j (t)||≥d_safe,∀i,j∈N_i,∀t 				(3-15)

其中p_i (t)、p_j (t)为第i、j架战机在时刻t的位置向量（二维平面坐标）， d_safe 为最小安全距离。

(5) <font color="red">编队接敌节奏约束</font>

<font color="red">同一编队内的战机不要求严格同时到达，但不应形成过大的接敌时序离散：</font>
|T_i-T_j |≤ΔT_sync,∀i,j∈N_i 							(3-16)

<font color="red">其中 ΔT_sync 为允许的最大接敌时间差。该约束在当前实现中作为节奏参考约束使用，主要用于避免责任飞机接入顺序被进一步拉大，而不再作为显式速度优化问题单独求解。</font>

(6) 目标区域覆盖约束

所有战机的引导路径需覆盖敌方可能出现的置信区域：

⋃_(i=1)^N▒ {p_i (t):t∈[0,T_i]}⊇R_enemy 					(3-17)

其中 R_enemy 为敌方可能出现的置信区域（考虑预警机误差和敌方分布范围）。

(7) 探测资源约束

为保持体制一致性，单机稳定跟踪目标数不超过 $N_{\text{trk}}^{\max}=6$；进入责任构造和扫描分配的态势级候选目标数按不超过10个控制。该约束用于考核系统在“最小资源投入”条件下维持期望跟踪状态（态势级或火控级）的能力。

3.3.3分层近似求解思路

为使加权代价函数与后文算法一一对应，本文在算法设计中采用“目标函数—子问题—求解流程”的一致化映射：将加权代价函数拆解为若干可在线求解的子目标，并在滚动时域内近似最优。

<font color="red">时间最优项 T_max：对应第3.4节“规划层+协调层”。算法3.4给出编队级引导目标点 p_i^* 与 θ_i^* 的构造，算法3.5在此基础上对各机速度进行协调优化，使责任飞机在满足飞行包线和编队节奏约束的前提下，以较高速度尽快接近目标区域，并抑制过大的接敌时序离散，从而在滚动在线框架下支撑最小截获时间目标。</font>

资源代价项 C_resource：对应第3.5节 “雷达资源调度”。其实现本质是将扫描模式、扫描半宽和多机责任展开视为离散资源决策，在满足目标发现与保持要求的前提下，降低扫描周期、覆盖角以及冗余责任展开带来的探测资源占用。

丢失概率项 P_miss：对应第3.4.3节“预测层+区域层”与第3.5.2节“搜索模式”。式(3-22a)–(3-22d)把敌方机动与信息间歇丢失映射为置信域膨胀 σ_man (t)，从而把“丢失风险”转化为可计算的覆盖约束（式(3-17)、式(3-31)/(3-32)/(3-33)）。

<font color="red">本文采用分层滚动决策框架（每周期按“信息→估计→预测→区域→规划→协调→执行”顺序更新），并结合离散候选集上的枚举与代价比较进行在线求解：例如探测模式按信息可用性与预测上下文在 {DIRECTED, SEARCH, SWEEP} 中切换（第3.5.2节），扫描体制在 {NARROW, MEDIUM, WIDE} 集合内按责任跨度自适应选取，责任分散中心角与主/支搜索关系由第3.5.3节的责任分配结果确定，航向/速度由算法3.4/3.5 给出。该方式用离散搜索与解析构造替代全局连续优化，每一步均由明确的目标与约束驱动，满足实时性要求。下面第3.4节在上述模型与约束基础上，给出协同探测决策的具体算法设计。</font>

3.4协同探测决策算法设计

根据第3.3节建立的优化目标与约束条件，协同探测决策采用分层滚动方式：在每个决策周期内基于当前置信域与约束更新编队引导与速度协调；式(3-10)为理想化的全局优化目标，因实时性与可解性约束，本文通过滚动实施在有限时域内逼近该目标，而非保证全局最小。本章给出上述问题的分层滚动求解框架及信息可用性评估、敌方编队估计、机动不确定性传播与置信域预测、编队级路径引导、速度协调与动态路径调整等核心算法。

3.4.1总体架构与滚动决策主循环

<font color="red">为突出“从预警引导到 R_radar 门限”的主任务，并显式处理“敌方航向时变”的机动不确定性，本文采用分层滚动决策框架：每个决策周期 k（典型 0.2s）基于最新预警信息与己方状态，更新敌方置信域并重新计算编队级引导目标与速度协调策略；当编队中任一战机与敌方中心距离不大于 R_radar 时，切换至第3.5节雷达扫描分配流程。总体流程如下。</font>

算法 3.2 协同探测分层滚动决策主循环（0–R_radar 主任务）

<pre>
输入：预警机航迹 Z_k（目标位置与最后更新时间），我方状态 S_k^F（各机位置、航向、速度），当前时间 t_k。
输出：每机飞行引导指令 u_(i,k)^fly=(θ_i^*,v_i^*)；若已进入 R_radar 门限则含雷达扫描指令 u_(i,k)^scan。
1   Q_awacs ← 按式(3-18)计算；target_lost ← 按式(3-19)判据 // 信息层：预警质量与丢失标志
2   〖p̂〗_enemy ← 基于 Z_k 更新（第3.4.3（1）节）// 估计层：敌方编队中心
3   σ_man (t_k) ← 算法3.3 // 预测层：机动不确定性传播与置信半径
4   σ_enemy (t_k), R_target (t_k) ← 第3.4.3（2）节 // 区域层：分布范围与目标区域半径
5   {*_p^iⓜ,*_θ^i } ← 算法3.4（编队级引导目标与航向） // 规划层：每机目标点与目标航向
6   <font color="red">{*_v^i } ← 算法3.5（速度协调优化）// 协调层：按责任接入次序与目标距离生成可执行速度档位</font>
7   for i ← 1 to N do 〖fly〗_u^i ← 将 (*_θ^i, *_v^i) 转为航向/速度控制量（第3.4.5节）// 执行层：目标航向/速度→控制量
8   <font color="red">if 〖min⁡〗_i ∥p_i-p ̂_enemy∥ ≤ R_radar then 进入第3.5节扫描分配，得到 〖scan〗_u^i // 门限判断：到位则切雷达扫描</font>
9   return {〖fly〗_u^i }（及可选的 〖scan〗_u^i）
</pre>

<font color="red">上述算法表明，每个决策周期（如 0.2 s）按“信息→估计→预测→区域→规划→协调→执行”顺序更新一次：先评估预警机信息质量与目标是否丢失，再估计敌方中心与置信域，接着用算法3.3/3.4/3.5 得到编队级引导目标、航向与速度指令，并转为单机控制量；当任一战机与敌方中心距离不大于 R_radar 时切换至第3.5节雷达扫描流程。该主循环将机动不确定性通过“预测层→区域层”映射为可计算的置信域，在预警丢失或敌方机动时仍能引导编队到位。</font>

从作战阶段视角看，协同探测可视为超视距作战中的“接敌引导+目标搜索”阶段：主循环持续完成信息更新、敌情预测与编队引导机动，并在进入雷达作用距离后转入搜索确认流程。该阶段输出的编队空间态势、目标置信域与单机控制指令，可作为后续火控/交战决策阶段的统一初始条件与状态输入，从而保证后续研究与本报告在同一基础模型下连续衔接。

3.4.2信息可用性评估与不确定性量化

（1）预警机信息质量评估

在 0–R_radar 阶段，尚未形成机载雷达高精度航迹，难以对空战态势优势与威胁做完整评估。因而本文在设计层面仅引入信息可用性与可靠性评估，即对预警机信息的可用性、精度与时效性进行量化，为后续“置信域构造—路径规划—速度协同”提供输入。定义预警机信息质量指标：

Q_awacs=clip(1-σ_awacs/d_min ,0,1) 									(3-18)

其中 d_min=〖min⁡〗_j∥p_j^awacs-p_formation∥ 为编队中心到各预警目标的最小距离，clip⁡(a, 0, 1) 表示将 a 限制在 [0ⓜ,1] 内。该指标反映预警机信息精度相对于目标距离的比值，值越大表示信息质量越高。给定预警机目标位置集合 {p_j^awacs } 与我方编队中心 p_formation，先计算 d_min，再按式(3-18)得到 Q_awacs。

（2） 目标丢失检测

目标丢失检测是触发SEARCH模式的关键。定义目标丢失判据：

〖target〗_lost={█(&True,    &&if (max)┬j (t_current-t_j^last)>T_lost@&False,    &&otherwise    &&)┤			 (3-19)

<font color="red">其中 t_j^last 为目标 j 的最后更新时间，T_lost 为丢失判定阈值。对每个目标维护其最近一次预警更新时间戳 t_j^last。给定当前时间 t_current，若存在目标满足 (t_current-t_j^last)>T_lost，则判定该目标预警信息已丢失并置 target_lost=True；否则 target_lost=False。其输出用于滚动引导的保守化以及第3.5.2节 SEARCH 模式的触发。</font>

3.4.3敌方编队中心与分布范围估计

（1）敌方编队中心估计

敌方编队中心估计是协同探测决策引导路径规划的基础。考虑预警机位置误差为 σ_awacs、各目标探测误差相互独立，采用等权重算术平均方法估计编队中心：

p ̂_enemy=1/M ∑_(j=1)^M▒  p_j^awacs 							(3-20)

等权重平均在探测误差相同时可最大化信息利用效率；估计误差为 σ_estimate=σ_awacs/√M（多目标平均降低误差，符合统计规律）。当预警机仅探测到部分目标时，该估计方法仍能给出合理的中心估计。

（2）敌方分布范围估计

敌方分布范围估计需综合考虑编队空间分布、预警机误差与机动不确定性。定义分布范围指标：

σ_enemy (t)=max(σ_spatial (t),σ_awacs)+σ_man (t) 			(3-21)

其中：

σ_spatial=√(1/M ∑_(j=1)^M▒〖∣∣〗 p_j^awacs-p ̂_enemy∣∣^2 )：目标位置的空间分散度（当 M=1 时取默认值 σ_default，由编队空间分布设定）；

σ_awacs：预警机位置误差（作为分布范围的下界）；

σ_man (t)：机动不确定性增量（由无量测预测与机动模型引起的时变膨胀项，见第（3）节）；

置信区域半径为 R_target=k_σ⋅σ_enemy，其中 k_σ 为覆盖系数（对应给定置信度下的覆盖）。编队空间分布自适应处理为，当预警机仅探测到部分目标时，根据探测到的目标数量推断编队空间分布：M=1 时取 σ_default（密集排列）；M=2 时取 σ_default（中等间距）；M≥4 时使用实际计算的 σ_spatial。σ_default 的具体取值在仿真中根据场景设定。

图3.2 敌方中心估计与目标区域估计示意

蓝色散点表示预警测量散布，黑点为中心估计，蓝色圆为目标区域半径 。该示意强调“测量越散/机动越强 → 区域越大”。）

（3）机动不确定性传播与置信域预测

由于敌方机动不确定性，会存在预警间歇丢失和连续机动的问题，预警间歇丢失指预警机更新不及时或链路中断，在一段时间内无新量测；连续机动指目标在该段时间内持续机动（如转弯、加速），其真实位置相对上次估计的偏差会随时间增大。若仅以“当前预警位置分散度 + 固定余量”构造目标区域，无法覆盖“连续机动 + 预警间歇丢失”情形下目标可能到达的范围。为此，本节将敌方机动不确定性显式建模为无量测预测下的协方差增长，并将其转化为随时间膨胀的置信域。

对目标 j 的状态估计 (x ̂_(j,k)ⓜ,P_(j,k) )，在 τ=t-t_k 的无量测预测窗口内（例如预警更新周期、间歇丢失窗口），其预测协方差按滤波预测方程传播：

├ x ̂_j (t|t_k)=f(x ̂_(j,k),τ),P_j (t|t_k )=F_j (τ)P_(j,k) F_j (τ)^T+Q_j (τ)		 (3-22a)

其中 F_j (τ) 为线性化雅可比（或模型矩阵），Q_j (τ) 为过程噪声协方差，反映机动强度上界（CV/CT/CA/Singer 等模型由IMM加权融合）。提取二维位置协方差子矩阵 P_(pos,j) (t∣t_k)∈R^(2×2)，定义 γ 置信度（自由度2）对应的卡方阈值2_χ^γ，则位置置信椭圆满足：

ε_j (t)={p:(p-p ̂_j )^T P_(pos,j) (t|t_k )^(-1) (p-p ̂_j)≤2_χ^γ} 			(3-22b)

后续路径规划中目标区域采用圆形（半径 R_target）描述；置信椭圆为任意朝向的椭圆。为与圆形目标区域一致，将椭圆用其外接圆近似，半径取椭圆最大半轴对应的置信半径：

r_j (t)=√(χ_p^2⋅λ_max (P_(pos,j) (t|t_k)))							 (3-22c)

并定义机动不确定性增量为所有目标外接圆半径的上界：

σ_man (t)=(max)┬j r_j (t) 								(3-22d)

据此，式(3-22)中的 σ_man (t) 自动体现“机动越强/丢失越久，目标区域越大”的规律；预警恢复或雷达量测进入后，P_pos 收缩，σ_man (t) 随之减小，从而使引导策略自适应收敛。

算法3.3 机动不确定性传播与置信域预测算法

```text
输入：各目标当前状态与协方差 {(x ̂_(j,k),P_(j,k),t_k)}，当前时间 t，置信度 p（如 0.99），IMM 过程噪声 {Q_m }。
输出：机动不确定性增量 σ_man (t)，各目标预测置信半径 {r_j (t)}。
1   for j ← 1 to M do
2       τ_j← t − t_k // 按式(3-22a)：用 IMM 无量测预测得到 t 时刻的位置协方差（从全状态协方差提取 2×2 位置子块）
3       P_(pos,j) (t|t_k) ← IMM 预测位置协方差(x ̂{j,k}, p{j,k},τ_j, {Q_m} )
4       r_j (t) ←√(χ_p^2⋅λ_max (P_(pos,j) (t|t_k)))  // 式(3-22c)：置信椭圆外接圆半径 = 卡方阈值 × 位置协方差最大特征值的平方根
5   σ_man (t) ←(max)┬j r_j (t)   // 式(3-22d)：机动不确定性增量取所有目标置信半径的上界
6   return (σ_man (t), {r_j (t)})
```

从上述算法可以看出，在无新量测的时间段内，用 IMM 预测方程将各目标位置协方差向前传播，得到 t 时刻的位置不确定性椭圆；再按给定置信度 p 用最大特征值构造外接圆半径 r_j (t)，并取所有目标的上界作为 σ_man (t)，供第3.4.3（2）节构造 R_target。预警更新或雷达量测进入后协方差会收缩，σ_man (t) 随之减小，引导策略自适应收敛。

3.4.4编队级引导目标与航向分配算法

（1）目标区域确定

基于估计的敌方编队中心 p ̂_enemy 和分布范围 σ_enemy，确定目标区域 R_target 为以 p ̂_enemy 为中心、半径为 R_target 的圆形区域。

R_target={p:∥p-p ̂_enemy∥≤R_target} 							(3-23)

其中 R_target=k_σ⋅σ_enemy，k_σ=3 对应三倍标准差意义上的覆盖。

图3.3 编队引导几何关系示意

<font color="red">上图展示了编队级引导目标与航向分配（下述算法3.4）的核心几何关系：编队中心指向敌方中心的方向作为引导基准，目标区域随敌方分布与机动不确定性自适应变化。当前工程实现并非在远距离阶段就把长机/僚机直接分到圆边界两侧，而是先沿敌方中心方向压缩接敌；待距离缩短后，再围绕目标区域做渐进展开，最终形成近侧/远侧包夹目标点。</font>

（2）编队级引导目标

在编队级引导目标生成之前，系统首先完成协同探测任务分配。该分配过程采用“责任方向—飞机”的责任映射：预警有效阶段，AWACS 提供带误差的目标方位集合，系统优先组织4架飞机对4个目标方向形成首轮责任覆盖；预警失效或目标连续机动阶段，分配结果转为“预测责任区间—飞机”的覆盖映射，用于确定当前哪些飞机承担主搜索、哪些飞机承担补盲或接续覆盖。

任务分配遵循三项原则。第一，优先可用原则：优先选取当前朝向责任区、处于热段且火控雷达前向可视区能够直接覆盖目标方位的飞机承担主责任；第二，最短发现时间原则：若多架飞机都可覆盖，则优先选择转向代价更小、到达目标区域时间更短的飞机先行接管；第三，最小资源原则：在责任方向已被覆盖的前提下，其余飞机不再无差别重复扫描，而是转为补盲、分散覆盖或转向接入，从而避免无效的资源重叠。

在预警信息有效时，系统依据目标方位集合构造责任方向集合，并结合敌方中心估计结果和我方飞机当前朝向，优先将4个目标责任方向分配给当前迎敌向且几何条件较优的飞机。目标方位跨度较小时，4个责任方向相互接近，系统将扫描责任压缩在较小方位范围内，以较窄扇区实现快速截获；目标方位跨度增大或目标呈宽正面分布时，责任方向随之展开，多机在责任方向附近做有限分散，形成对更宽责任区间的协同覆盖。该责任映射随目标方位分布和我方飞机可用性滚动更新。

在预警信息失效或目标连续机动导致空间预测困难时，分配对象由瞬时目标点切换为预测方位中心及其置信区间。此时系统首先依据 IMM-EKF 的无量测预测结果得到目标可能出现的方位范围，再判断当前哪些飞机仍处于迎敌向、哪些飞机的前向火控视场可以优先覆盖预测区间。考虑当前双机对和冷热交替巡逻构型，初始阶段通常仅有2架左右迎敌向飞机具备直接搜索条件，因此这部分飞机先承担主搜索责任，其余飞机在转向到位后承担补盲或接续覆盖任务。该机制使“预警失效”不再等同于“全队同时宽扫”，而是转化为“有限可用飞机先行搜索、其余飞机延后接入”的有序责任展开。

编队级引导目标与航向分配需考虑编队关系、责任覆盖和到位节奏约束。本文在二维平面内处理航向与相对几何；编队中心到敌方中心的直接航向、各机目标点与目标航向均在水平面内计算。该算法首先计算编队中心到敌方中心的直接航向，然后结合编队关系和责任区间分配各机目标位置，最后在必要时通过速度协调对同编队飞机的到位节奏做有限修正。

<font color="red">在当前实现中，责任分配结果并不只用于后续扫描中心分配，还会直接回灌到编队引导层。算法3.4先生成基于敌方中心与置信区域的基础引导点</font> $p_{i,\text{base}}$ <font color="red">，再根据责任方位构造责任引导点</font> $p_{i,\text{resp}}$ <font color="red">，并按主责/支援角色与当前距离加权融合为最终引导点</font> $p_{i,\text{guide}}$。

$$
\color{red}{
p_{i,\text{guide}}
=
\left(1-\omega_i\right)p_{i,\text{base}}
+\omega_i p_{i,\text{resp}}
}
$$

<font color="red">其中，主责飞机在远距离阶段取较大的融合权重，随距离缩短逐步回收；支援飞机的融合权重更小。进入 SEARCH 后，该责任权重再统一乘以</font> $0.35$ <font color="red">的缩放系数，使飞行引导在保持责任指向的同时不过度僵硬。责任方向因此不仅决定“扫哪里”，也直接塑形“飞向哪里”。</font>

算法3.4 编队级引导目标与航向分配算法

<pre style="color:red">输入：我方4机位置 {p_i}，估计敌方中心 p_enemy_hat，置信区域半径 R_target，编队关系 F，
      责任方位映射 responsibility_map，责任角色映射 responsibility_role_map
输出：每架战机的目标航向 {θ_i*}、目标速度 {v_i*}、目标位置 {p_i_guide}
1   p_formation <- (1/4) Σ_i p_i
2   for each (lead, wing) in F do
3       p_pair <- (p_lead + p_wing) / 2
4       e_pair <- (p_enemy_hat - p_pair) / ||p_enemy_hat - p_pair||
5       d_pair <- ||p_enemy_hat - p_pair||
6       half_R_eff <- min(R_target/2, 120, 0.45*d_pair)
7       if d_pair >= 220 then w_bracket <- 0
8       else if d_pair <= 140 then w_bracket <- 1
9       else w_bracket <- (220 - d_pair) / 80
10      offset <- w_bracket * half_R_eff
11      p_base_lead <- p_enemy_hat - offset * e_pair
12      p_base_wing <- p_enemy_hat + offset * e_pair
13  for each fighter i do
14      if i 有责任方位 then
15          沿责任方位构造责任引导点 p_i_resp
16          按主责/支援角色、距离与模式确定权重 ω_i
17          p_i_guide <- (1-ω_i) p_i_base + ω_i p_i_resp
18      else
19          p_i_guide <- p_i_base
20      θ_i* <- atan2(p_i_guide - p_i)
21      {v_i*} <- 算法3.5 // 速度协调优化，按责任接入次序输出离散速度档位
22  return ({θ_i*}, {v_i*}, {p_i_guide})</pre>

<font color="red">上述算法在当前实现中对应“基础包夹引导 + 责任方向塑形”两步。第一步先根据敌方中心和目标区半径生成基础包夹引导，其中远距离阶段采用压缩接敌，距离进入</font> $220\,\text{km}\rightarrow140\,\text{km}$ <font color="red">区间后逐步展开，且用</font> $120\,\text{km}$ <font color="red">与</font> $0.45d_{\text{pair}}$ <font color="red">共同约束展开半径，避免远距离阶段直接把僚机拉到过远侧。第二步再根据责任方向对引导点做修正：主责飞机的责任融入更强，主责责任点前视距离取</font> $100\sim260\,\text{km}$<font color="red">；支援飞机的责任融入较弱，前视距离取</font> $80\sim180\,\text{km}$<font color="red">；若进入 SEARCH，则该责任修正整体缩弱到原来的</font> $0.35$<font color="red">。因此，算法3.4并不是理想化的静态对称分配，而是围绕“先快速接敌、再逐步展开、并与责任方位保持一致”组织当前工程实现。</font>

<font color="red">上述</font> $220\,\text{km}$<font color="red">、</font>$140\,\text{km}$<font color="red">、</font>$120\,\text{km}$<font color="red">和</font> $0.45d_{\text{pair}}$ <font color="red">都属于当前实现中的工程整定参数，而不是从理论公式直接推导出的固定常数。它们的作用分别是：把远距离阶段限制为压缩接敌；把中近距离阶段切换为逐步展开；把单侧展开半径限制在当前双机对几何可承受范围内；并避免展开半径相对当前敌距过大。当前三类场景的初始接敌距离主要分布在240~320 km量级，若在该距离段过早执行完整对称展开，僚机目标点会被推向敌方远侧，容易出现绕飞和首轮接敌变慢；若始终不展开，又无法在中近距形成左右责任分担。因而本节采用这组参数，将开局控制为“先压缩接敌”，将后续控制为“再逐步展开”。在答辩中可将这组参数解释为：针对当前平台性能、当前三类场景尺度和当前双机对构型完成的几何整定结果，其目标是保证首轮接敌稳定，并为后续责任展开留下空间；若平台性能或初始距离发生明显变化，则应重新整定。</font>

3.4.5速度协调辅助算法

<font color="red">速度协调在本文中作为接敌引导阶段的辅助优化环节。其任务是在责任方向、责任飞机和引导目标点已经确定后，结合各机到责任区的距离、主/支责任关系以及任务阶段速度边界，对速度做有限协调：主责任飞机优先保持较高接敌速度，支援飞机按接入次序做小幅调节，随后统一映射为控制层可直接执行的离散速度档位。这样处理的目的，是在不破坏责任覆盖结构的前提下尽快完成4目标发现，并抑制同一编队内过大的接敌时序离散。</font>

<font color="red">（1） 速度协调优化问题</font>

<font color="red">设第</font> $i$ <font color="red">架战机到其责任目标点的距离记为</font> $d_i=\lVert p_i^*-p_i\rVert$<font color="red">，任务阶段给出的基准速度记为</font> $v_{\text{phase}}$<font color="red">，责任修正项记为</font> $\Delta v_{\text{role},i}$<font color="red">，则连续速度参考值写为：</font>

$$
\color{red}{
\tilde{v}_i=\operatorname{clip}\!\left(
v_{\text{phase}}+k_d d_i+\Delta v_{\text{role},i},
v_{\min},
v_{\max}
\right)
}
\tag{3-24a}
$$

<font color="red">其中</font> $k_d$ <font color="red">为距离增益系数，</font> $\Delta v_{\text{role},i}$ <font color="red">用于体现主/支责任差异：承担主责任方向或主搜索责任的飞机取正修正量，以便更快进入有效探测几何；支援飞机取较小修正量，使其在不脱离责任结构的前提下完成后续接入。在此基础上，再将连续速度参考值映射到控制层支持的离散速度档位集合</font> $V_{\text{cmd}}=\{150,180,200,220,250,280,300,330\}$<font color="red">（m/s）中：</font>

$$
\color{red}{
v_i^*=\arg\min_{v\in V_{\text{cmd}}}\left|v-\tilde{v}_i\right|
}
\tag{3-24b}
$$

<font color="red">其中 clip(·) 表示边界裁剪算子，v_i^* 为最终输出的离散速度指令。该处理方式的核心含义是：速度层不单独决定责任分配，而是在责任已经确定之后，为“谁先进入有效探测几何、谁负责后续补齐”提供节奏层面的辅助优化。</font>

<font color="red">算法3.5 速度协调优化算法</font>

```text
输入：各机当前位置 {p_i}，责任目标点 {p_i*}，责任角色 {role_i}，
      任务阶段基准速度 v_phase，速度范围 [v_min, v_max]，
      离散速度档位集合 V_cmd
输出：离散速度指令 {v_i*}
1   for i = 1 to N do
2       d_i <- ||p_i* - p_i||               // 当前飞机到责任目标点的距离
3       根据 role_i 确定 delta_v_role_i    // 责任速度修正量：主责任较大，支援较小
4       v_ref_i <- clip(v_phase + k_d * d_i + delta_v_role_i, v_min, v_max)
5       v_i* <- 在 V_cmd 中选取与 v_ref_i 最接近的速度档位
6   return {v_i*}
```

<font color="red">在给定各机当前位置、责任目标点和责任角色后，算法3.5先依据距离和责任先后关系生成连续速度参考值，再映射为最近的离散速度档位输出。该处理方式使速度层能够参与“尽快发现全部目标”的节奏优化，但又不改变上一层已经确定的责任覆盖结构。</font>

<font color="red">其中，伪代码中的</font> `delta_v_role_i` <font color="red">对应式(3-24a)中的责任修正项，</font> `v_ref_i` <font color="red">对应连续速度参考值；在伪代码中改用这两个名称，是为了提高可读性，含义与式(3-24a)、式(3-24b)保持一致。</font>

3.4.6单机控制量生成

本节对应基础需求文档中“战术机动动作数学模型及控制指令求解”的执行侧实现，属于该章分层控制架构在“协同探测阶段”的任务化裁剪，而非独立控制模型。基础模型的分层链路为“战术决策层→轨迹/引导层→指令离散化层→控制转换层→物理仿真层”；其中，算法3.4/3.5给出编队级目标航向与速度，本节完成其向单机可执行控制量的离散化映射，即在每个决策周期输出航向变化率与加速度指令。

与基础模型第4章相比，本节不展开复杂复合机动原语与全轨迹规划，仅处理协同探测所需的航向/速度引导执行；但所采用的状态描述、控制量定义、约束边界与离散执行机制保持一致，因而全文基础模型同源同构。

本节将算法3.4/3.5输出的目标航向与目标速度转换为每周期可执行的航向/速度控制量，并满足航向变化率与加速度约束，起到“决策输出—执行层”的衔接作用。单机控制量生成模块将算法3.4和3.5输出的目标航向 θ_i^* 和目标速度 v_i^* 转换为航向/速度调整命令，该转换过程需满足航向变化率限制和加速度限制，以保证飞行控制的平滑性与稳定性。

航向跟踪控制律：计算航向误差 Δθ=θ_i^*-θ_i^current，并将其归一化到 [-180°,180°] 区间（若 Δθ>180° 则减去360°，若 Δθ<-180° 则加上360°）。采用带限幅的比例控制，限制航向变化率：

Δθ_cmd=clip(Δθ,-Δθ_max⋅Δt,Δθ_max⋅Δt) 							(3-25)

其中 Δθ_max 为最大航向变化率（度/秒），Δt 为控制周期。

速度跟踪控制律：计算速度误差 Δv=v_i^*-v_i^current，采用带限幅的比例控制，限制加速度：

Δv_cmd=clip(Δv,-a_max⋅Δt,a_max⋅Δt) 							(3-26)

其中 a_max 为最大加速度限制。该跟踪策略保证航向/速度平滑过渡，避免控制量突变导致的飞行不稳定。

3.4.7 动态路径调整算法

当发生预警更新、敌方机动、预警丢失或目标丢失等事件时，需对当前路径与置信域进行更新或切换扫描模式；本节给出在上述事件触发下的动态路径调整与模式切换逻辑。

（1） 路径调整触发条件

动态路径调整的触发条件定义如下事件：

预警机信息更新事件：在时刻 t 接收到新的预警观测包，且其观测时间戳（或序号）发生变化。记当前最新预警观测时间戳为 t_awacs^msg，上一轮已处理时间戳为 t_awacs^proc，则：

event_update⇔(t_awacs^msg≠t_awacs^proc) 										(3-27)

同时定义预警信息时效性量：

Δt_awacs=t-t_awacs^msg 										(3-28)

其中 Δt_awacs 为预警信息时效量，在延迟/丢失判据中使用。

敌方机动检测事件：在窗口 [t-ΔT_m, t] 内，若估计航向/速度变化满足

(max)┬j |Δψ_j |≥ψ_th 或 (max)┬j |Δv_j |≥v_th										 (3-29)

其中 Δψ_j、Δv_j 由 IMM-EKF 输出的速度矢量变化计算得到，ψ_th、v_th 为机动阈值参数。

预警机信息延迟/丢失事件：当 Δt_awacs 超过系统允许的信息时效阈值时认为预警信息已过期或丢失：

event_stalc⇔Δt_awacs>T_awacs,event_lost⇔Δt_awacs>T_(lost,awacs) 		(3-30)

其中 T_awacs 为预警更新周期（或其等效上界），T_(lost,awacs) 为丢失阈值（通常取其若干倍）。若系统显式给出 I_awacs∈{0,1}，则也可用 I_awacs=0 直接触发 event_lost。

目标丢失事件：target_lost=True（第3.4.2（2）节）

（2）动态路径调整算法

算法3.6 动态路径调整算法

<pre>
输入：当前路径 {θ_i^currentⓜ,v_i^current }，预警机信息与事件标志，当前时间 t。
输出：调整后的路径 {θ_i^newⓜ,v_i^new }。
1   if 预警机信息更新 then
2       <font color="red">更新  p ̂_enemy（第3.4.3（1）节）；σ_man (t), R_target ← 算法3.3、第3.4.3（2）节</font>
3        {*_p^iⓜ,*_θ^i }  ← 算法3.4；{*_v^i }  ← 算法3.5
4   else if 敌方机动检测 then
5     σ_man (t) , R_target ← 算法3.3、第3.4.3（2）节（必要时R_target ← κ_man R_target）
6       {*_p^iⓜ,*_θ^i }  ← 算法3.4；{*_v^i } ← 算法3.5
7   else if 预警机信息丢失 then
8       <font color="red">飞行层维持滚动引导（IMM 更新 σ_man）；雷达层切换至 SEARCH（第3.5.2节）</font>
9   else if 目标丢失 then
10      切换至 SEARCH，以最后已知状态做无量测预测构造搜索置信域（算法3.3、3.5.3（3）节）
11  for i ← 1 to N do
12       〖new〗_θ^i ← α 〖current〗_θ^i + (1−α) *_θ^i；〖new〗_v^i ← α 〖current〗_v^i + (1−α) *_v^i
13  return {〖new〗_θ^i, 〖new〗_v^i}
</pre>

<font color="red">根据“预警更新 / 敌方机动 / 预警丢失 / 目标丢失”四类事件，决定是否重新计算敌方中心、置信域、引导目标与速度指令；若需重算则调用算法3.3/3.4/3.5，若为预警丢失或目标丢失则在飞行层继续滚动引导的同时将雷达层切至 SEARCH。最后对航向与速度做一阶平滑（系数 α），避免路径突变，输出平滑后的 {θ_i^newⓜ,v_i^new }。动态调整后重新得到的目标航向 *_θ^i 与目标速度 *_v^i分别由算法3.4和算法3.5在当前时刻的置信域与约束下求解，与主循环一致，在滚动意义下继续逼近式(3-10)；平滑系数 α 用于避免指令突变，不改变式(3-10)的逐周期优化结构。</font>

3.5雷达扫描分配设计

当编队中任一战机与敌方中心距离不大于 R_radar 时，由第3.4节主循环（算法3.2）切换至本章雷达扫描分配流程。本章给出探测模式决策SWEEP/DIRECTED/SEARCH 三种模式下的扫描任务分配算法，以及扫描范围的计算方法。扫描模式设计必须满足平台给定的机载火控雷达体制参数，该参数在算法设计中视为硬约束。

3.5.1雷达参数定义

本章的扫描模式设计满足给定的机载火控雷达体制参数（扫描半宽、俯仰行数对应扫描周期），本文统一采用“扫描半宽”为 ±10^∘/±30^∘/±60^∘ 的三档方位扫描，并通过选择俯仰行数得到对应扫描周期。

表3-1 机载火控雷达工作状态

| 方位半宽 | 俯仰1行（3°） | 俯仰2行（5°） | 俯仰4行（10°） |
| --- | --- | --- | --- |
| ±10^∘ | 2 s | 4 s | 8 s |
| ±30^∘ | 5 s | 10 s | 20 s |
| ±60^∘ | 10 s | 20 s | 40 s |

同时，雷达对 5 m^2 RCS 目标的最大探测距离为 $R_{\text{radar}}=200\,\text{km}$，单机最大可同时稳定跟踪目标数为 $N_{\text{trk}}^{\max}=6$。

3.5.2探测模式决策

协同探测系统定义三种探测模式：

表3-2 探测模式

| 模式名称 | 英文 | 触发条件 | 雷达扫描模式 | 决策目标 |
| --- | --- | --- | --- | --- |
| <font color="red">推磨扫描</font> | <font color="red">SWEEP</font> | <font color="red">无预警信息且无可用预测上下文</font> | <font color="red">MEDIUM（±30^∘）</font> | <font color="red">在前方空域保持均匀基础覆盖</font> |
| <font color="red">定向扫描</font> | <font color="red">DIRECTED</font> | <font color="red">有预警机目标方位</font> | <font color="red">按责任跨度在 NARROW / MEDIUM / WIDE 中自适应选取</font> | <font color="red">以最小有效扫描区间快速完成截获</font> |
| <font color="red">智能搜索</font> | <font color="red">SEARCH</font> | <font color="red">目标丢失，或无预警但仍保留可用预测上下文</font> | <font color="red">按预测责任区间在 MEDIUM / WIDE 中自适应组织主搜索与补盲</font> | <font color="red">基于预测责任区间完成搜索恢复</font> |

<font color="red">有预警目标方位时采用定向扫描，但定向扫描并不固定等同于窄扫，而是先把“目标方位跨度 + 不确定性扩张”折算为有效责任跨度，再在 NARROW、MEDIUM、WIDE 三种体制中选择满足覆盖要求的最小可用体制；目标丢失时立即切换至智能搜索，依托第3.4节的置信域预测把目标可能出现的方位范围转化为责任搜索区间，并由几何条件更优的飞机优先承担主搜索；仅当预警不可用且历史/融合/预测上下文也不足以支撑责任搜索时，才退化为推磨扫描，在指定扇区内做均匀覆盖。</font>

图3.4 探测模式切换关系示意

<font color="red">三块区域分别对应三类扫描模式；箭头表示典型触发链路：预警可用时趋向“定向”；目标丢失或预警不可用但仍保留预测上下文时转入“搜索”；仅当预警不可用且预测上下文也不足时才回到“推磨”。图中仅保留“模式与切换结构”，具体触发条件与体制参数由表3-1决定。</font>

3.5.3扫描任务分配

<font color="red">扫描任务分配的本质是“责任方向/责任区间—飞机”的责任映射。当预警信息有效时，系统优先依据目标方位集合完成快速责任分配，使各飞机尽快形成对4个目标的责任覆盖；当预警信息失效时，分配对象由瞬时目标点切换为预测方位中心与置信区间，责任形式由目标分配转为扇区分配。进入责任构造前，候选目标先按置信度和时间戳排序，并截断为不超过10个态势级目标。考虑当前双机对与冷热巡逻构型，初始阶段通常由迎敌向、具备较好火控雷达几何条件的飞机优先承担主搜索责任，其余飞机在转向到位后承担补盲或接续覆盖任务。因此，本节给出的扫描中心、扫描半宽和俯仰行数，对应的是上一层“任务分配结果”的执行化表达。前一层先确定责任方向和责任飞机，本节再把这种责任映射落实为“哪架飞机扫哪里、扫多宽、扫多快”。</font>

<font color="red">在工程实现上，系统先把目标方位或预测方位整理为有限个责任槽位，再求解“飞机—责任槽位”的最小代价匹配。对第</font> $i$ <font color="red">架飞机分配第</font> $s$ <font color="red">个责任槽位的代价可写为</font>

$$
\color{red}{
J_{i,s}
=
w_h\left|\Delta\theta_{i,s}\right|
+w_f P_{\text{forward},i,s}
+w_l P_{\text{lane},i,s}
-w_p\pi_s
}
\tag{3-34}
$$

<font color="red">其中</font> $\Delta\theta_{i,s}$ <font color="red">为当前航向与责任方位的偏差；</font> $P_{\text{forward},i,s}$ <font color="red">表示当前不具备前向直接覆盖几何时的惩罚项；</font> $P_{\text{lane},i,s}$ <font color="red">表示飞机横向位置与责任方位左右次序不一致时的惩罚项；</font> $\pi_s$ <font color="red">为槽位优先级。系统在全部可行匹配中选取总代价最小的一组分配结果，再把该结果转写为扫描中心、扫描半宽和俯仰行数。这样做的直接效果是：责任分配不再按飞机编号平均摊开，而是按“谁更朝向目标、谁更容易先形成有效探测几何”来组织扫描责任。</font>

在选定探测模式（SWEEP、DIRECTED 或 SEARCH）后，需为每架战机分配扫描中心与扫描半宽，且须满足表3-1的约束。本节按三种模式分别给出分配策略与所用公式。

（1）推磨扫描（SWEEP）

当无预警机信息时，采用推磨扫描策略，使多机在指定扇区内均匀覆盖，避免漏检任意方向上的目标。模式与体制绑定为 MEDIUM，即扫描半宽 $\theta_{\text{range}}=30^\circ$（表3-1）。各机扫描中心限制在可行扇区 $\left[-\Theta_{\text{total}}/2+\theta_{\text{range}},\,\Theta_{\text{total}}/2-\theta_{\text{range}}\right]$ 内均匀布置：中心间距取

$$
\Delta\theta_{\text{sweep}}
=
\max\left(0,\frac{\Theta_{\text{total}}-2\theta_{\text{range}}}{N-1}\right)
$$

第 $i$ 机扫描中心为

$$
\theta_{\text{center},i}
=
-\frac{\Theta_{\text{total}}}{2}
+\theta_{\text{range}}
+(i-1)\Delta\theta_{\text{sweep}}
$$

其中 $\Theta_{\text{total}}$ 为总覆盖扇区宽度（如 $120^\circ$）。例如 $N=4$、$\Theta_{\text{total}}=120^\circ$ 时，$\theta_{\text{center},i}\in\{-30^\circ,-10^\circ,10^\circ,30^\circ\}$，四机覆盖并集为 $\left[-60^\circ,60^\circ\right]$。

（2）定向扫描（DIRECTED）

<font color="red">当有预警机目标方位信息时，采用定向扫描策略，使多机扫描中心集中在目标方向附近，以缩短截获时间。先由预警目标方位集合得到方位中心与跨度</font> $\theta_{\text{center}}$<font color="red">、</font> $\theta_{\text{span}}$<font color="red">，再结合敌方中心估计与当前置信半径折算方位不确定性半宽</font> $\theta_{\text{unc}}$<font color="red">，由此构造有效责任跨度</font>

$$
\color{red}{
\Theta_{\text{eff}}
=
\theta_{\text{span}}
+2\theta_{\text{unc}}
+\Delta\theta_{\text{count}}
}
\tag{3-35}
$$

<font color="red">其中</font> $\Delta\theta_{\text{count}}$ <font color="red">为目标数量修正项，当前实现取值为：4目标时加</font> $10^\circ$<font color="red">，3目标时加</font> $6^\circ$<font color="red">，其余取0。随后按</font> $\Theta_{\text{eff}}$ <font color="red">选择满足覆盖要求的最小雷达体制：当</font> $\Theta_{\text{eff}}\ge 55^\circ$ <font color="red">时采用 WIDE（方位半宽</font> $\pm60^\circ$<font color="red">、俯仰2行、扫描周期20 s）；当</font> $24^\circ\le \Theta_{\text{eff}}<55^\circ$ <font color="red">，或责任目标数达到3个及以上时采用 MEDIUM（方位半宽</font> $\pm30^\circ$<font color="red">、俯仰2行、扫描周期10 s）；其余采用 NARROW（方位半宽</font> $\pm10^\circ$<font color="red">、俯仰1行、扫描周期2 s）。也就是说，定向扫描并不是固定“窄扫”，而是把预警方位集合压缩为最小有效扫描区间后，再选择最省资源但足以覆盖的体制。</font>

<font color="red">在确定扫描体制后，系统把各目标方位整理为责任槽位，并按式(3-34)求解“飞机—责任槽位”的最小代价分配。航向更接近责任方位、当前处于迎敌向、横向位置与责任方位左右次序更一致的飞机，优先承担对应责任槽位；未承担独立责任槽位的飞机不做无差别重复扫描，而是在中心方位附近承担支援覆盖。当前实现还给出了与体制对应的多机责任分散中心角：NARROW、MEDIUM、WIDE 分别对应</font> $4^\circ$<font color="red">、</font> $8^\circ$<font color="red">、</font> $16^\circ$ <font color="red">的中心偏置量。这里的</font> $4^\circ/8^\circ/16^\circ$ <font color="red">用于错开多机扫描中心，不是单机雷达自身的方位半宽。</font>

<font color="red">对于远距离开局且预警方位高度收敛的情形，系统引入 early compact opening 逻辑：当当前时刻不超过45 s、编队到敌方参考距离不小于260 km、目标方位跨度不超过</font> $8^\circ$<font color="red">、有效责任跨度不超过</font> $18^\circ$<font color="red">，且并非4架飞机都已具备迎敌几何时，不强行让4机同时展开首轮扫描，而是先由当前朝向更优的少数飞机承担首轮主扫描，其余飞机保留支援角色，待转向到位后再接续接入。这样处理避免了远距离开局阶段因为平均铺开而破坏首轮可用几何，也与老师提出的“先尽快发现，再考虑后续持续覆盖”要求保持一致。</font>

图3.5 多机雷达扫描扇区与责任区划分示意

（3）智能搜索（SEARCH）

<font color="red">当目标丢失时，采用智能搜索策略，基于第3.4节机动不确定性传播与置信域预测，在方位上覆盖目标可能出现的区域。对每个目标利用 IMM 无量测预测得到预测位置与位置协方差（算法3.1与3.3），进而得到相对编队中心的预测方位</font> $\theta_j^{\text{pred}}$ <font color="red">以及方位不确定性半宽</font>

$$
\theta_{\text{unc},j}=\arctan\!\left(\frac{r_j(t)}{d_j}\right)
$$

<font color="red">其中</font> $d_j$ <font color="red">为编队中心到该目标预测位置的距离，</font> $r_j(t)$ <font color="red">为置信半径（式(3-22c)）。系统将各预测方位整理为责任槽位，再按式(3-34)求解“飞机—责任槽位”的主搜索分配，使当前朝向更优、转向代价更小的飞机先承担主搜索责任。</font>

<font color="red">搜索模式下并非固定采用全机统一宽扫，而是先根据预测方位跨度与不确定性半宽构造搜索责任区间，再按距离压缩程度决定采用中等扇区还是宽扇区。若目标仍集中在有限方位范围内，且已进入近距恢复阶段，则优先用中等扇区组织主搜索，以更高刷新效率完成补齐；若不确定性继续膨胀或目标预测跨度明显展开，则再扩展为宽扇区，并由其余转向到位的飞机承担补盲或接续覆盖。只有在预测上下文也不足时，系统才退回SWEEP做均匀基础搜索。由此，SEARCH 的核心并不是“扫得更宽”，而是“把预测误差转换为有限责任搜索区间，再按几何条件组织主搜索与补盲”。</font>

3.5.4扫描范围的计算

将目标位置不确定性（长度量纲）映射为角度域的扫描宽度时，可采用以下两种方式，二者均在体制允许的 NARROW–WIDE 角度范围内截断。

基于误差圆的简化方法：若将目标位置误差视为半径 $r_{\text{error}}$ 的圆（$r_{\text{error}}$ 取预警机误差 $\sigma_{\text{awacs}}$ 或雷达误差 $\sigma_{\text{radar}}$），战机到目标距离为 $d$，则扫描角度半宽可取为 $\arctan(r_{\text{error}}/d)$，即

$$
\theta_{\text{scan}}
=
2\arctan\!\left(\frac{r_{\text{error}}}{d}\right)
\tag{3-31}
$$

基于协方差椭圆的方法：当采用 IMM-EKF 等估计器时，目标位置不确定性由协方差矩阵 $P$ 描述。提取二维位置协方差子矩阵 $P_{\text{pos}}$，做特征值分解

$$
P_{\text{pos}}=V\Lambda V^{T}
\tag{3-32}
$$

在给定置信度 $p$（如 $0.99$）下，椭圆半轴为 $a=\sqrt{\chi_p^2\lambda_{\max}}$、$b=\sqrt{\chi_p^2\lambda_{\min}}$（$\chi_p^2$ 为自由度 2 的卡方阈值）。以长半轴对应角度加余量作为扫描半宽：

$$
\theta_{\text{scan}}
=
2\arctan\!\left(\frac{a}{d}\right)
\tag{3-33}
$$

若需再留余量可加数度（仿真中给定）。最终扫描范围需限制在表3-1中NARROW 到 WIDE 对应的角度范围内。第3.6节在上述模型与分配策略基础上，针对具体仿真对象与初始态势给出参数取值与验证指标体系。

3.6仿真验证设计

<font color="red">第3.1-3.5节给出了协同探测决策与雷达扫描分配的模型与算法。本节采用同场景对照实验验证当前实现的有效性，并重点回答两个问题：其一，在预警信息有效时，责任方向分配是否能够更快完成4目标发现；其二，在预警失效或低空突防导致空间预测困难时，基于预测责任区间的搜索机制是否能够优于固定扫描方式。</font>

3.6.1 仿真平台与参数设置

（1）平台与时间离散

飞行力学采用JSBSim作为仿真内核，提供六自由度飞行动力学模型。对比实验统一采用固定步长0.2 s，总步数2100步，对应仿真时长420 s。

（2）参试对象与传感器量级

我方为4架苏-27，敌方为4架F-16，均配挂导弹4枚。关键传感器量级如下：

火控雷达（我方N001VE）：探测距离约200 km，稳定跟踪距离约170 km，锁定距离约120 km；

火控雷达（敌方APG-68）：探测距离约105 km，稳定跟踪距离约85 km，锁定距离约70 km；

预警机：探测距离400 km，位置精度约2.5 km，更新周期10 s。

（3）CAP任务几何与参考系

CAP任务责任区保持一致，责任区为200 km × 300 km，靶眼坐标为(100, 300) km。态势示意统一采用“北向为上”的俯视坐标表达，采用NED（北-东-地）坐标系。

<font color="red">（4）对比方案与可复现性控制</font>

<font color="red">本节采用同场景、同机动、同随机种子的严格对照设计：3个固定场景（V01-V03），2种方法共6次仿真。同一场景下，本文方法与对照方法使用相同敌方初始配置、相同敌方机动脚本、相同预警失效时间线和相同随机种子，因此结果差异可归因于协同探测与扫描分配策略本身。</font>

<font color="red">对比方案说明如下：</font>

<font color="red">本文方法（PROPOSED）：采用当前实现的七层协同探测流程，包括信息层、估计层（IMM-EKF）、预测层（机动不确定性传播）、区域层（责任区间与置信区域计算）、规划层（编队引导）、协调层（速度协调优化）和执行层（动态路径调整）。预警信息有效时，按目标方位集合和飞机几何关系形成责任方向分配；预警信息失效但仍保留可用预测上下文时，转入SEARCH并按预测责任区间组织搜索。</font>

<font color="red">对照方法（BASELINE）：不进行机动不确定性传播与预测责任区间构造。预警信息有效时采用基础定向扫描；预警信息失效时退化为固定SWEEP覆盖，不根据预测方位区间重新组织多机责任。</font>

<font color="red">因此，两者的主要差异不在于平台条件，而在于是否显式建模不确定性、是否把预测结果转化为责任搜索区间、以及是否进行责任方向分配。</font>

3.6.2 验证场景设计

<font color="red">为覆盖“预警有效引导”“宽正面责任展开”“低空断续预警搜索恢复”三类关键工况，本节设置由易到难的三组对头来袭场景（V01-V03）。三场景共同点为：敌方初始航向均为180°，我方保持双机对冷热交替巡逻，敌方早期机动脚本均在前段触发并最终回正。差异主要体现在编队宽度、高度剖面和预警信息可用性。</font>

表3-3 验证场景设计
| 场景 | 编队形态/高度/预警状态 | 关键验证点 |
| --- | --- | --- |
| <font color="red">V01</font> | <font color="red">标准编队 + 中高度 + 预警连续可用</font> | <font color="red">验证预警有效条件下的责任方向分配能否更快完成首探与全探</font> |
| <font color="red">V02</font> | <font color="red">宽正面 + 中高度 + 预警连续可用</font> | <font color="red">验证宽正面分布下责任展开与覆盖自适应能力</font> |
| <font color="red">V03</font> | <font color="red">宽正面 + 低空起始后跃升 + 预警多次短时失效</font> | <font color="red">验证预测责任区间搜索对低空掉线和断续预警的恢复能力</font> |

<font color="red">敌方初始位置配置为：V01采用标准编队，横向间隔约20 km量级；V02采用宽正面编队，横向展开约120 km量级；V03同样采用宽正面，但初始高度降至3.5 km，并在早期阶段跃升至9 km，同时叠加多次预警短时失效窗口。V01、V02属于约300 km量级远距离接敌工况，V03属于240 km量级压缩距离工况。</font>

三场景对应的早期机动序列分别为：

<font color="red">V01：两编队括号机动 + 9/7 km分层 + 单侧钩形 + 回正；</font>

<font color="red">V02：分裂 + 交叉趋势 + 高度层对换（7/10 km）+ 回正；</font>

<font color="red">V03：异步跃升 + 折线机动 + 回正，并叠加多次预警短时失效窗口。</font>

<font color="red">三类场景在算法验证上的分工也不同。V01用于验证“预警有效时能否把目标方位集合快速转化为责任方向”；V02用于验证“当目标横向展开时，责任方向是否能够及时展开到外侧目标”；V03用于验证“当预警短时中断、低空突防和连续机动叠加时，预测责任区间搜索能否替代固定宽扫并维持整体发现链”。因此，三场景并不是对同一问题的简单重复，而是分别对应责任分配、责任展开和搜索恢复三类关键能力。</font>

3.6.3评价指标定义

<font color="red">结合老师对“突出方法有效性、但不要把过程指标无节制铺满”的要求，本节采用“分组定义、分场景择重点展开”的评价体系。全部指标仍在单机稳定跟踪不超过6个、责任构造候选目标不超过10个的资源约束下统计。</font>

<font color="red">1. 时效性指标：</font>

<font color="red">首次锁定时间（s）：首次出现任一战机建立有效航迹的时刻，反映系统由预警引导进入雷达截获的总体时效。</font>

<font color="red">雷达进入工作阶段平均时刻（s）：4架飞机雷达进入主工作阶段时刻的平均值，反映编队整体进入雷达主导阶段的早晚。</font>

<font color="red">雷达进入工作阶段同步性（标准差，s）：4架飞机雷达激活时刻的标准差，反映编队接入节奏是否离散。</font>

<font color="red">平均首探延迟（s）：各目标从雷达进入工作阶段到首次建立有效航迹的平均延迟，反映扫描组织效率。</font>

<font color="red">首探延迟标准差（s）：各目标首探延迟的离散程度，反映各目标被拉起的均衡性。</font>

<font color="red">全目标探测补齐用时（s）：首个目标建立有效航迹后，完成4目标全部建立有效航迹所需的额外时间。</font>

<font color="red">联队完成全目标探测时刻（s）：从仿真开始到4目标全部建立有效航迹的总时间。</font>

<font color="red">2. 连续性/覆盖指标：</font>

<font color="red">激活后全覆盖连续性（0-1）：雷达主导阶段内，4目标同时被持续覆盖的时间占比。</font>

<font color="red">激活后任意目标连续性（0-1）：雷达主导阶段内，至少有1个目标被持续覆盖的时间占比。</font>

<font color="red">激活后平均覆盖率（0-1）：雷达主导阶段内，被有效覆盖目标数量占4的时间平均值。</font>

<font color="red">3. 策略行为指标：</font>

<font color="red">模式切换次数（次）：DIRECTED、SEARCH、SWEEP 间的切换总次数。</font>

<font color="red">各模式驻留时间（s）：各探测模式的累计驻留时间。</font>

<font color="red">搜索覆盖累计时间（s）：SEARCH 或 SWEEP 状态下的累计时间，用于表征预警失效窗口内的恢复成本。</font>

<font color="red">4. 协同效率指标：</font>

<font color="red">战机负载基尼系数（0-1）：各机工作负载分布的离散程度，越小表示负载越均衡。</font>

<font color="red">冗余探测比率（0-1）：多机同时对同一目标实施探测的时间占比，反映资源重叠程度。</font>

<font color="red">目标分配基尼系数（0-1）：各机承担目标责任的集中程度，用于观察责任是否过度聚集在少数飞机上。</font>

<font color="red">为避免每个场景都机械堆满全部指标，下文主表统一报告与“发现速度 + 覆盖保持 + 协同效率”最相关的指标；模式切换与模式驻留时间重点在V03展开，用于说明预警失效窗口内的搜索恢复方式。</font>

3.6.4 场景V01：预警有效条件下的责任方向分配验证

图3.6 V01场景对照方法示意图

图3.7 V01场景本文方法示意图

（1）场景配置与验证目标

<font color="red">V01为标准对头接敌场景，敌方采用标准编队，中高度来袭，预警信息连续可用，场景中不设置预警失效窗口。该场景不重点考察搜索恢复，而是考察在预警方位信息稳定可用的情况下，责任方向分配是否能够直接转化为更快的首探与全探。</font>

（2）结果与机制分析

<font color="red">从最新对照结果看，V01中本文方法与对照方法在420 s仿真窗口内均始终处于DIRECTED模式，模式切换次数均为0。本场景中的性能差异直接对应于预警有效条件下的责任方向分配和定向扫描组织方式。</font>

<font color="red">从过程上看，V01可以分为三个连续阶段。第一阶段为开局引导接敌阶段，0~约210 s 内预警信息始终有效，责任方向围绕较窄的目标方位集合展开，本文方法在该阶段不需要进入SEARCH或SWEEP，而是持续用DIRECTED组织责任压缩。第二阶段为首轮截获阶段，本文方法在210.2 s建立首个有效航迹，对照方法在215.2 s建立首个有效航迹，差距为5.0 s。第三阶段为全探补齐阶段，本文方法自首锁到全探完成用时32.8 s，而对照方法需要44.4 s，说明差距主要不在“是否能先碰到一个目标”，而在“首锁之后能否更快把其余责任方向补齐”。</font>

<font color="red">本文方法的首次锁定时间为210.2 s，早于对照方法的215.2 s；联队完成全目标探测的时刻为230.8 s，早于对照方法的250.4 s。全目标探测补齐用时由44.4 s缩短到32.8 s，说明本文方法在首轮建立航迹后，能够更快完成其余目标的接续补齐。</font>

<font color="red">从各目标最佳首探结果看，本文方法对B0200的最佳首探延迟为19.6 s，而对照方法为30.8 s；对B0300的最佳首探延迟分别为8.0 s和9.2 s。该差异表明，在预警有效条件下，本文方法能够更早将目标方位集合转写为责任方向，使迎敌向飞机优先覆盖本侧责任区。对应地，本文方法的激活后平均覆盖率达到0.899，高于对照方法的0.854；全覆盖连续性达到0.852，高于对照方法的0.711；冗余探测比率为0.642，略低于对照方法的0.655，说明其在保持覆盖的同时减少了无效重叠。</font>

<font color="red">从过程看，V01中两种方法都没有经历预警中断，因此不存在SEARCH与SWEEP之间的恢复差异。两者区别集中在进入雷达工作阶段后的责任组织方式。本文方法先按预警方位集合整理责任方向，再按飞机朝向和左右几何关系分配责任槽位，使一部分飞机优先对准本侧责任方向，另一部分飞机承担相邻责任方向或支援覆盖。对照方法虽然同样依赖预警方位，但没有显式的责任槽位分配和最小代价匹配，因此更容易在中心方位附近形成重复扫描，而对偏侧目标的接续覆盖稍慢。</font>

<font color="red">这一点可以从目标补齐过程上看出。V01中本文方法并不是只在首锁时刻略占优势，而是在首个目标建立有效航迹后，后续目标补齐更快，最终把全目标探测补齐用时压缩了11.6 s。尤其是B0200的首探延迟差达到11.2 s，说明责任方向分配并未停留在“先看到一个目标”的层面，而是确实改善了偏侧目标的接入效率。这正对应了第3.5.3节所述的责任映射作用：预警方位被压缩为有限责任槽位后，扫描资源不再平均摊开，而是优先投向当前最容易形成有效探测几何的方向。</font>

<font color="red">需要指出的是，V01中本文方法的雷达激活标准差为11.8 s，对照方法为10.8 s，本文方法并未在所有辅助指标上占优。因此，V01的主要收益并不来自雷达更早同时开启，也不来自单纯提高飞行速度，而是来自预警有效条件下责任方向分配后，扫描责任落实得更快、更集中、更少无效重叠。</font>

（3）指标对比

<font color="red">表3-4 场景V01核心指标对比</font>
| 指标 | 本文方法 | 对照方法 | 对比结论 |
| --- | --- | --- | --- |
| <font color="red">首次锁定时间（s）</font> | <font color="red">210.2</font> | <font color="red">215.2</font> | <font color="red">本文方法提前5.0 s</font> |
| <font color="red">雷达进入工作阶段平均时刻（s）</font> | <font color="red">214.8</font> | <font color="red">221.2</font> | <font color="red">本文方法提前6.4 s</font> |
| <font color="red">雷达进入工作阶段同步性（标准差，s）</font> | <font color="red">11.8</font> | <font color="red">10.8</font> | <font color="red">对照方法略优，差异不构成主导因素</font> |
| <font color="red">平均首探延迟（s）</font> | <font color="red">14.2</font> | <font color="red">16.5</font> | <font color="red">本文方法缩短2.3 s</font> |
| <font color="red">首探延迟标准差（s）</font> | <font color="red">5.2</font> | <font color="red">8.7</font> | <font color="red">本文方法更均衡</font> |
| <font color="red">全目标探测补齐用时（s）</font> | <font color="red">32.8</font> | <font color="red">44.4</font> | <font color="red">本文方法缩短11.6 s</font> |
| <font color="red">联队完成全目标探测时刻（s）</font> | <font color="red">230.8</font> | <font color="red">250.4</font> | <font color="red">本文方法提前19.6 s</font> |
| <font color="red">激活后全覆盖连续性</font> | <font color="red">0.852</font> | <font color="red">0.711</font> | <font color="red">本文方法提高0.141</font> |
| <font color="red">激活后任意目标连续性</font> | <font color="red">0.945</font> | <font color="red">0.957</font> | <font color="red">对照方法略高，但差异很小</font> |
| <font color="red">激活后平均覆盖率</font> | <font color="red">0.899</font> | <font color="red">0.854</font> | <font color="red">本文方法提高0.045</font> |
| <font color="red">冗余探测比率</font> | <font color="red">0.642</font> | <font color="red">0.655</font> | <font color="red">本文方法重叠更少</font> |
| <font color="red">战机负载基尼系数</font> | <font color="red">0.042</font> | <font color="red">0.093</font> | <font color="red">本文方法负载更均衡</font> |

<font color="red">V01表明：在预警信息连续可用的基础场景下，本文方法的优势集中体现在责任方向分配环节，搜索恢复机制在本场景中未构成主要增益来源。</font>

3.6.5 场景V02：宽正面条件下的责任展开验证

图3.8 V02场景对照方法示意图

图3.9 V02场景本文方法示意图

（1）场景配置与验证目标

<font color="red">V02在V01基础上将敌方编队扩展为宽正面，并加入分裂、交叉趋势和高度层对换等早期机动。该场景重点考察敌方横向展开后，责任方向能否快速展开到外侧目标，避免多机在中部方位集中造成边缘目标补齐滞后。</font>

（2）结果与机制分析

<font color="red">最新对照结果显示，V02中两种方法同样在全程保持DIRECTED模式，模式切换次数均为0。因此，本场景考察的是“预警有效 + 宽正面”的责任展开能力，搜索恢复能力在本场景中不构成主导因素。</font>

<font color="red">从过程上看，V02同样可以分为三个阶段。第一阶段为宽正面接敌阶段，敌方横向展开、分裂和交叉趋势使目标方位跨度明显大于V01，责任分配不再只是在中心方位附近压缩，而需要尽早向外侧展开。第二阶段为首轮截获阶段，本文方法在221.4 s建立首个有效航迹，对照方法在231.8 s建立首个有效航迹。第三阶段为外侧目标补齐阶段，本文方法在270.8 s完成4目标全探，对照方法在295.4 s完成，差距主要体现在外侧责任方向能否提前接入，而不是体现在中心目标是否能被发现。</font>

<font color="red">本文方法的首次锁定时间为221.4 s，对照方法为231.8 s；联队完成全目标探测的时刻为270.8 s，对照方法为295.4 s。与V01相比，V02的宽正面几何使全目标补齐难度明显上升，但本文方法仍将全目标探测补齐用时从73.4 s压缩到57.2 s，说明其在首轮进入雷达工作阶段后，对外侧目标的接续覆盖更快。</font>

<font color="red">这一点在各目标最佳首探结果中表现得更明显。对于最外侧目标B0400，本文方法的最佳首探延迟为39.2 s，对照方法为54.2 s，差距达到15.0 s。该结果说明，在宽正面分布下，本文方法没有把扫描责任长期压在中心附近，而是更早将责任展开到外侧方位。对应地，本文方法的激活后平均覆盖率为0.826，高于对照方法的0.795；全覆盖连续性为0.635，高于对照方法的0.621；冗余探测比率为0.568，低于对照方法的0.638，说明多机覆盖分工更有效而非更集中。</font>

<font color="red">从过程机制上看，V02与V01最大的不同在于目标横向跨度显著增大，且早期存在分裂和交叉趋势。如果仍按中心方位组织扫描，外侧目标会在首轮被压后，直到后续飞机补转向后才逐步被拉起。本文方法在该场景中的作用，正是把“宽正面”从几何现象转化为责任展开问题：当预警方位跨度和不确定性折算后的有效责任跨度增大时，定向扫描不再局限于窄扫，而会扩展到更宽的扫描体制，并将责任槽位向外侧铺开。</font>

<font color="red">B0400的结果最能说明这一点。本文方法对该外侧目标的最佳首探延迟为39.2 s，而对照方法为54.2 s，差距达到15.0 s。与此同时，本文方法对B0200的最佳首探延迟仅为2.2 s，对照方法为9.8 s，说明责任展开并没有以牺牲近侧目标为代价，而是在内外两侧同时取得收益。由此可以认为，V02中的主要改进并不是简单把所有飞机都扫得更宽，而是在宽正面下更早把责任方向展开到外侧，并减少中部责任重叠。</font>

<font color="red">V02中本文方法的雷达激活平均时刻为228.0 s，对照方法为233.9 s；激活标准差分别为9.3 s和10.5 s。与V01相比，这里激活时刻和激活离散度均有改善，说明在宽正面条件下，责任展开不仅改善了目标补齐速度，也改善了各机接入雷达工作阶段的整体节奏。</font>

（3）指标对比

<font color="red">表3-5 场景V02核心指标对比</font>
| 指标 | 本文方法 | 对照方法 | 对比结论 |
| --- | --- | --- | --- |
| <font color="red">首次锁定时间（s）</font> | <font color="red">221.4</font> | <font color="red">231.8</font> | <font color="red">本文方法提前10.4 s</font> |
| <font color="red">雷达进入工作阶段平均时刻（s）</font> | <font color="red">228.0</font> | <font color="red">233.9</font> | <font color="red">本文方法提前5.9 s</font> |
| <font color="red">雷达进入工作阶段同步性（标准差，s）</font> | <font color="red">9.3</font> | <font color="red">10.5</font> | <font color="red">本文方法更同步</font> |
| <font color="red">平均首探延迟（s）</font> | <font color="red">23.5</font> | <font color="red">26.9</font> | <font color="red">本文方法缩短3.4 s</font> |
| <font color="red">首探延迟标准差（s）</font> | <font color="red">18.7</font> | <font color="red">17.3</font> | <font color="red">对照方法略低，但不改变总体结论</font> |
| <font color="red">全目标探测补齐用时（s）</font> | <font color="red">57.2</font> | <font color="red">73.4</font> | <font color="red">本文方法缩短16.2 s</font> |
| <font color="red">联队完成全目标探测时刻（s）</font> | <font color="red">270.8</font> | <font color="red">295.4</font> | <font color="red">本文方法提前24.6 s</font> |
| <font color="red">激活后全覆盖连续性</font> | <font color="red">0.635</font> | <font color="red">0.621</font> | <font color="red">本文方法提高0.014</font> |
| <font color="red">激活后任意目标连续性</font> | <font color="red">0.962</font> | <font color="red">0.951</font> | <font color="red">本文方法略优</font> |
| <font color="red">激活后平均覆盖率</font> | <font color="red">0.826</font> | <font color="red">0.795</font> | <font color="red">本文方法提高0.031</font> |
| <font color="red">冗余探测比率</font> | <font color="red">0.568</font> | <font color="red">0.638</font> | <font color="red">本文方法重叠更少</font> |
| <font color="red">战机负载基尼系数</font> | <font color="red">0.069</font> | <font color="red">0.056</font> | <font color="red">对照方法略均衡，但本文方法外侧展开收益更明显</font> |

<font color="red">V02表明：当敌方目标从标准编队扩展为宽正面分布时，本文方法的收益主要体现在外侧目标接入更早、责任重叠更少，以及全探补齐速度更快。</font>

3.6.6 场景V03：低空断续预警条件下的预测搜索验证

图3.10 V03场景对照方法示意图

图3.11 V03场景本文方法示意图

（1）场景配置与验证目标

<font color="red">V03为低空起始、早期跃升、预警多次短时失效的压缩场景，初始距离为240 km。该场景对应老师最关注的工况：预警失效后不能简单依赖固定扫描，而需要根据已有历史/融合信息和预测区间重新组织搜索责任。</font>

（2）结果与机制分析

<font color="red">V03中，两种方法都经历了相同的预警失效时间线，模式切换次数同为12次，但失效窗口内的工作方式不同：本文方法在38.0 s的失效阶段采用SEARCH，对照方法在同样38.0 s的失效阶段采用SWEEP。V03的区分点体现在失效窗口内所采用的搜索策略。</font>

<font color="red">从过程上看，V03可以分为三个阶段。第一阶段为压缩距离接敌阶段，初始距离仅240 km，敌方又以低空起始后跃升，导致首轮雷达接入窗口明显提前，本文方法与对照方法都必须在更短时间内完成由预警引导向雷达主导的切换。第二阶段为预警间歇失效阶段，双方经历相同的12次模式切换和总计38.0 s的失效窗口，但本文方法把这38.0 s用于SEARCH，对照方法把这38.0 s用于固定SWEEP。第三阶段为全探恢复阶段，本文方法在131.0 s完成4目标全探，对照方法在162.6 s完成，说明差距主要体现在失效窗口内的恢复组织方式，而不是体现在是否都遇到了预警掉线。</font>

<font color="red">从首轮结果看，本文方法的首次锁定时间为95.4 s，略慢于对照方法的92.6 s。这说明固定SWEEP在压缩距离场景中有可能更早碰到某个近侧目标。但该优势没有延续到整体4目标发现链路。本文方法的全目标探测补齐用时为39.4 s，而对照方法为80.4 s，差距达到41.0 s；联队完成全目标探测的时刻分别为131.0 s和162.6 s，本文方法整体提前31.6 s；平均首探延迟由33.8 s下降到25.2 s。该结果表明，SEARCH依据预测责任区间组织搜索后，虽然未必最早碰到第一个目标，但能够更快把其余目标整体拉起。</font>

<font color="red">这一点在外侧目标上表现得尤为明显。对B0400而言，本文方法的最佳首探延迟为38.4 s，而对照方法为67.6 s；差距达到29.2 s。说明断续预警条件下，本文方法能够把预测方位区间转化为责任搜索区间，使搜索资源优先投向后续最难补齐的目标方向。</font>

<font color="red">从场景机理看，V03同时叠加了三个不利因素：一是初始距离压缩到240 km，首轮探测反应时间显著缩短；二是敌方由低空起始后异步跃升，导致相对方位和高度关系在早期快速变化；三是预警信息存在多次短时中断，使系统不能持续依赖新的预警方位。对照方法在这类场景中会退化为固定SWEEP，因此虽然可能更早碰到近侧单个目标，但由于扫描责任没有围绕预测责任区间重新组织，后续外侧目标和远侧目标更容易补齐滞后。</font>

<font color="red">本文方法在该场景中的作用则体现在：预警短时中断后，系统并不直接退化为全机均匀宽扫，而是利用历史航迹、融合结果和IMM预测结果，先恢复预测方位中心与预测责任区间，再由几何条件更优的飞机承担主搜索责任，其余飞机在转向到位后承担补盲或接续覆盖。因此，V03中“首锁略慢”与“整体补齐显著更快”并不矛盾，前者反映固定宽扫在个别时刻可能先碰到近侧目标，后者反映预测责任区间搜索在整体4目标恢复上更有效。</font>

<font color="red">覆盖保持能力的差异同样明显。本文方法的激活后全覆盖连续性为0.694，对照方法为0.567；激活后平均覆盖率分别为0.844和0.811。与此同时，本文方法的雷达激活均值/标准差为92.7/1.2 s，对照方法为90.6/5.0 s。也就是说，对照方法虽然平均首锁略早，但其各机进入稳定工作状态的离散程度更大；本文方法则在进入雷达主导阶段后更快形成稳定的4目标覆盖链。这与第3.5.3节搜索模式中的主搜索/补盲分工是一致的。</font>

（3）指标对比

<font color="red">表3-6 场景V03核心指标对比</font>
| 指标 | 本文方法 | 对照方法 | 对比结论 |
| --- | --- | --- | --- |
| <font color="red">首次锁定时间（s）</font> | <font color="red">95.4</font> | <font color="red">92.6</font> | <font color="red">对照方法提前2.8 s</font> |
| <font color="red">雷达进入工作阶段平均时刻（s）</font> | <font color="red">92.7</font> | <font color="red">90.6</font> | <font color="red">对照方法略早，但差异不转化为整体全探优势</font> |
| <font color="red">雷达进入工作阶段同步性（标准差，s）</font> | <font color="red">1.2</font> | <font color="red">5.0</font> | <font color="red">本文方法明显更同步</font> |
| <font color="red">平均首探延迟（s）</font> | <font color="red">25.2</font> | <font color="red">33.8</font> | <font color="red">本文方法缩短8.6 s</font> |
| <font color="red">首探延迟标准差（s）</font> | <font color="red">13.2</font> | <font color="red">23.7</font> | <font color="red">本文方法更均衡</font> |
| <font color="red">全目标探测补齐用时（s）</font> | <font color="red">39.4</font> | <font color="red">80.4</font> | <font color="red">本文方法缩短41.0 s</font> |
| <font color="red">联队完成全目标探测时刻（s）</font> | <font color="red">131.0</font> | <font color="red">162.6</font> | <font color="red">本文方法提前31.6 s</font> |
| <font color="red">激活后全覆盖连续性</font> | <font color="red">0.694</font> | <font color="red">0.567</font> | <font color="red">本文方法提高0.127</font> |
| <font color="red">激活后任意目标连续性</font> | <font color="red">0.932</font> | <font color="red">0.969</font> | <font color="red">对照方法略高，但不能代表整体4目标恢复效果</font> |
| <font color="red">激活后平均覆盖率</font> | <font color="red">0.844</font> | <font color="red">0.811</font> | <font color="red">本文方法提高0.033</font> |
| <font color="red">冗余探测比率</font> | <font color="red">0.452</font> | <font color="red">0.511</font> | <font color="red">本文方法重叠更少</font> |
| <font color="red">战机负载基尼系数</font> | <font color="red">0.130</font> | <font color="red">0.056</font> | <font color="red">对照方法更均衡，但本文方法把资源更集中投向恢复关键方向</font> |
| <font color="red">模式切换次数（次）</font> | <font color="red">12</font> | <font color="red">12</font> | <font color="red">双方失效触发次数一致</font> |
| <font color="red">失效窗口工作方式</font> | <font color="red">SEARCH 38.0 s</font> | <font color="red">SWEEP 38.0 s</font> | <font color="red">本文方法把预测责任区间真正用于搜索恢复</font> |

<font color="red">V03表明：在预警失效和低空起始共同作用的困难条件下，本文方法的优势集中体现在预测责任区间搜索后更快完成4目标整体恢复，并保持更稳定的后续覆盖。</font>

3.6.7实验总结

<font color="red">综合V01-V03的对照结果，可以得到如下结论。</font>

<font color="red">（1）预警有效场景下，收益主要来自责任方向分配</font>

<font color="red">V01、V02全程均未进入搜索恢复分支，两种方法都保持DIRECTED模式。此时本文方法仍在首次锁定、全目标补齐和平均首探延迟上持续优于对照方法，说明预警有效条件下的责任方向分配本身已经能够带来首探与全探收益。</font>

<font color="red">（2）断续预警场景下，收益主要来自预测责任区间搜索</font>

<font color="red">V03中，两种方法都经历相同的模式切换次数，但本文方法在失效窗口采用SEARCH，对照方法采用固定SWEEP。结果表现为：本文方法首锁略慢，但4目标整体恢复速度、全覆盖连续性和平均覆盖率均明显更好，说明预测责任区间搜索是当前实现面对预警失效时的关键增益来源。</font>

<font color="red">（3）速度协调的作用是放大责任分配收益，而不是替代责任分配</font>

<font color="red">结合第3.4.5节和三场景结果可以看出，第三章的主要收益来源仍然是责任方向分配、宽正面责任展开，以及预警失效后的预测搜索责任组织；速度层的作用则是为主责任飞机提供更积极的接入节奏，为支援飞机提供较平滑的后续接入条件。也就是说，速度协调并不单独决定方法优劣，但它能够配合责任分配结果，减少接入过慢或无效等待对首探、全探时效的不利影响。</font>

<font color="red">表3-7 三场景关键结果汇总</font>
| 场景 | 首次锁定 | 全探延迟 | 平均首探延迟 | 全覆盖连续性 | 平均覆盖率 | 结论 |
| --- | --- | --- | --- | --- | --- | --- |
| <font color="red">V01</font> | <font color="red">210.2 < 215.2</font> | <font color="red">32.8 < 44.4</font> | <font color="red">14.2 < 16.5</font> | <font color="red">0.852 > 0.711</font> | <font color="red">0.899 > 0.854</font> | <font color="red">预警有效条件下的责任方向分配优于基础定向扫描</font> |
| <font color="red">V02</font> | <font color="red">221.4 < 231.8</font> | <font color="red">57.2 < 73.4</font> | <font color="red">23.5 < 26.9</font> | <font color="red">0.635 > 0.621</font> | <font color="red">0.826 > 0.795</font> | <font color="red">宽正面条件下外侧责任展开更有效</font> |
| <font color="red">V03</font> | <font color="red">95.4 > 92.6</font> | <font color="red">39.4 < 80.4</font> | <font color="red">25.2 < 33.8</font> | <font color="red">0.694 > 0.567</font> | <font color="red">0.844 > 0.811</font> | <font color="red">首锁略慢，但预测搜索使4目标整体恢复明显更强</font> |

<font color="red">因此，当前实现已经能够较清楚地验证两层核心能力：一是在预警有效时更快完成责任覆盖与目标发现；二是在预警失效时通过预测责任区间搜索维持更好的整体恢复能力与覆盖保持能力。</font>

