## 6.3 仿真验证

### 6.3.1 验证口径与说明

本文节的重点不是列举全部过程指标，而是围绕战术决策方法的有效性进行验证。所谓有效性，具体指：在不同敌情和信息条件下，系统是否能够在合适的控制距离节点选择合适的战术模板，是否能够在导弹威胁出现时及时转入规避，是否能够在威胁解除后恢复交战组织，以及这些决策是否带来了可解释的风险区控制效果和战果差异。

考虑到 stable-ready 后放行比值和接力制导比值在现有批次中容易受到聚合统计口径影响，出现长期恒定或明显失真的现象，故本节不再将其作为正式结论指标，而改用以下更稳定、也更便于解释的量：

- 首次 stable-ready 时间
- 首次发射门通过时间
- 高风险区累计突入时长
- 逐目标风险区进入历史
- 敌机击落数、我方损失数与交换比
- 雷达覆盖率与意图识别一致率

此外，各场景分析重点不同：场景一突出完整闭环与首轮交战组织，场景二突出信息不完整条件下的持续压制与恢复，场景三突出高风险压缩条件下的紧急规避和生存优先。这样才能使每个场景各自回答一个明确问题，而不是重复展示同一组指标。

图 6-1 给出本批次三场景总体结果对比。横轴为场景编号，纵轴为对应指标数值；蓝色为本文方法，灰色为基线方法。该图仅用于总览，不替代后续场景级决策分析。

![COMPARE_ALL_20260521_225438 三场景总体结果对比](figures/fig00_batch_summary.png)


### 6.3.2 场景一 低风险正面对进条件下的节点推进验证

#### 场景目标与初始条件

- 我方初始编组固定为四机 CAP 巡逻队形，A0100、A0300 初始航向为 0°，A0200、A0400 初始航向为 180°，初始高度约 9000 m，初始速度约 300 m/s，每机挂弹 4 枚。
- 敌方初始中心距离为 266.4 km，相对横向偏置分别为 [(-60.0, 0.0), (-48.0, 0.0), (48.0, 0.0), (60.0, 0.0)]，初始高度分别为 [10.0, 10.0, 10.0, 10.0] km，初始速度约 0.300 km/s。
- 预警支撑条件为：持续可用。场景重点为：重点分析模板首次选取、首轮发射窗口形成、导弹来袭后的规避触发与回转恢复。

本场景验证目标为：说明本文方法在低风险标准接敌条件下，能够较早形成 stable-ready，并按控制距离节点组织首轮攻击与规避回转。

图 S1-1 给出敌我初始几何关系与我方风险区示意。横轴为相对横向位置，纵轴为相对纵向位置；蓝色三角表示我方四机，红色圆点表示敌方四机，三条虚线圆分别表示低、中、高风险区。该图用于说明本场景开始时的几何约束和敌方初始突入方向。

![S1 初始几何与风险区示意](figures/S1/fig01_initial_geometry.png)

从图中可以看出，场景一的初始几何接近标准正面对进，后续差异主要来自决策方法而非开局位置偶然性。

#### 控制距离节点与战术模板选择分析

首次 stable-ready 出现在 105.0 s；首次发射门通过出现在 272.8 s，较 stable-ready 晚 167.8 s；首次接力成功出现在 300.0 s，较首次发射晚 27.2 s；首次击落出现在 342.2 s。

图 S1-2 给出逐目标风险区进入时序。横轴为时间，纵轴为敌方目标编号；蓝、橙、红三色分别表示目标处于低、中、高风险区。该图用于回答“哪一架敌机在什么时间进入了我方哪一级风险区”。

![S1 各敌机进入我方风险区的时间序列](figures/S1/fig02_zone_entry_timeline.png)

逐目标风险区演化记录如下：
- B0100 在 0.2 s 进入低风险区，240.2 s 进入中风险区。
- B0200 在 0.2 s 进入低风险区，240.2 s 进入中风险区。
- B0300 在 0.2 s 进入低风险区，240.2 s 进入中风险区。
- B0400 在 0.2 s 进入低风险区，240.2 s 进入中风险区。

图 S1-3 给出左右两路控制距离节点与关键事件时刻。横轴为时间，纵轴区分左路与右路；蓝色节点表示控制距离阶段首次进入时刻，虚线分别表示 stable-ready、首次发射门通过、首次接力成功、首次击落和首次损失。该图用于说明战术组织并不是在任意时刻发生，而是随控制距离节点推进而触发。

![S1 控制距离节点与关键事件时间关系](figures/S1/fig03_control_and_events.png)

从图中可以看出，stable-ready 与首次发射门通过之间存在约 167.8 s 的组织间隔，首次战果出现在 342.2 s，说明交战组织、发射放行与战果形成之间存在清晰的层次顺序。

结合 `decision_trace.csv` 的阶段理由、模板理由和参数理由，可以还原关键决策过程：

（1）100.2 s 时，系统处于 `ENGAGE` 状态。 左路采用 `DRAG_SHOOT` 指向 B0100，敌我参考距离约 199.2km，双机对目标距离约 188.6km，处于 BEYOND_NLT 节点，目标位于低风险区；说明系统并未急于收拢队形，而是优先保持射击准备与脱离余度并存，以便在较长距离上先建立攻击链、后保留回撤余地。 右路采用 `DRAG_SHOOT` 指向 B0400，敌我参考距离约 199.2km，双机对目标距离约 189.5km，处于 BEYOND_NLT 节点，目标位于低风险区；说明系统并未急于收拢队形，而是优先保持射击准备与脱离余度并存，以便在较长距离上先建立攻击链、后保留回撤余地。 发射放行前统一要求稳定跟踪至少持续 5.0 s；首轮发射窗口约束为 TR=70.1 km、LR=84.1 km；生存边界约束为 DOR=58.0 km、DR=54.0 km、MAR=36.7 km。 该节点仍属于远距接敌与站位塑形阶段，决策重点是建立感知和占位，而不是过早消耗火力。

（2）146.2 s 时，系统处于 `ENGAGE` 状态。 左路采用 `FRONT_BACK` 指向 B0100，敌我参考距离约 169.8km，双机对目标距离约 159.1km，处于 NLT_MELD 节点，目标位于低风险区；说明系统希望保持纵向前后配合，通过主攻与支援分工维持火力链连续性。 右路采用 `DRAG_SHOOT` 指向 B0300，敌我参考距离约 169.8km，双机对目标距离约 166.4km，处于 BEYOND_NLT 节点，目标位于低风险区；说明系统并未急于收拢队形，而是优先保持射击准备与脱离余度并存，以便在较长距离上先建立攻击链、后保留回撤余地。 发射放行前统一要求稳定跟踪至少持续 5.0 s；首轮发射窗口约束为 TR=69.0 km、LR=83.0 km；生存边界约束为 DOR=58.0 km、DR=54.0 km、MAR=36.6 km。 两路推进并非完全同步：左路位于 NLT_MELD，右路位于 BEYOND_NLT。 这表明系统允许左右两路围绕同一总任务进行分化推进，以同时兼顾主攻、支援和后续脱离余度。

（3）326.2 s 时，系统处于 `ENGAGE` 状态。 左路采用 `FRONT_BACK` 指向 B0100，敌我参考距离约 66.4km，双机对目标距离约 54.6km，处于 DR_MAR 节点，目标位于中风险区；说明系统希望保持纵向前后配合，通过主攻与支援分工维持火力链连续性。 右路采用 `FRONT_BACK` 指向 B0400，敌我参考距离约 66.4km，双机对目标距离约 60.0km，处于 TR_DOR 节点，目标位于中风险区；说明系统已接近首轮发射或脱离准备边界，采用前后分工以兼顾持续照射、脱离准备和回转余度。 发射放行前统一要求稳定跟踪至少持续 5.0 s；首轮发射窗口约束为 TR=61.4 km、LR=67.4 km；生存边界约束为 DOR=56.4 km、DR=52.4 km、MAR=38.4 km。 两路推进并非完全同步：左路位于 DR_MAR，右路位于 TR_DOR。 这表明系统允许左右两路围绕同一总任务进行分化推进，以同时兼顾主攻、支援和后续脱离余度。

上述记录表明，本场景中的模板切换并非由固定时间表驱动，而是由 `ctx_dist`、`pair_dist`、控制距离节点、风险区状态、预警支撑状态以及导弹来袭状态共同决定。记录中的 `first_window`、`survival` 和 `dynamic_ranges` 说明，动作参数不是任意给定，而是围绕发射窗口、生存边界和当前闭合态势动态修正的。

#### 典型时段剖析

典型时段选取 300 s 至 470 s。300.6 s 左路进入 LR_TR、右路进入 TR_DOR 后，319.8 s 左右两路同时转入 `FRONT_BACK`，表明系统已经不再以扩展搜索覆盖为主，而是围绕首轮攻击与脱离准备组织前后分工。 326.4 s 系统切换到 `EVADE`，其触发原因并非预设时长到达，而是目标距离已经压缩到 55.7 km 和 49.9 km，分别逼近当时给出的 `LR=56.0 km`、`TR=50.0 km` 和 `DOR=45.0 km` 约束。 也就是说，此时如果继续维持攻击姿态，后续可用规避空间将明显变窄，因此必须优先保留生存余度。 此后规避段持续到 456.6 s，时长约 130.4 s。 回转恢复同样不是按固定秒数结束，而是在敌我距离重新拉开、交战链具备再次组织条件后才发生；468.6 s 左路重新回到 `SIDE_BY_SIDE`，说明系统判定此时已经可以由规避状态转回协同跟踪和再次攻击准备。

#### 有效性对比

图 S1-4 给出本场景最需要关心的重点指标对比。横轴为对比方法，纵轴为对应指标数值；蓝色柱表示本文方法，灰色柱表示基线方法，柱顶数字为实际统计结果。该图采用时间节点、风险区占用和战果指标来评价决策效果，而不再依赖失真的聚合比值。

![S1 场景重点指标对比](figures/S1/fig04_focus_metric_compare.png)

从图中可以看出，本文方法的主要优势集中在前段组织效率和意图识别一致性，而最终击落数并未同步占优。

| 指标 | 本文方法 | 基线方法 | 对比结论 |
| --- | --- | --- | --- |
| 首次 stable-ready 时间 | 105.0 s | 214.8 s | 本文方法优于基线方法，差值为 109.8 s。 |
| 敌机击落数 | 4 | 2 | 本文方法优于基线方法，差值为 2.0。 |
| 我方损失数 | 0 | 0 | 两种方法在该指标上持平。 |
| 意图识别一致率 | 12.34% | 31.40% | 本文方法劣于基线方法，差值为 19.06 个百分点。 |
| 交换比 | 4.00 | 2.00 | 该指标用于最终战果对比，不单独作为节点决策优劣判据。 |

#### 场景结论

本文方法将首次 stable-ready 时间由 214.8 s 提前至 105.0 s，并将意图识别一致率由 31.40% 提升至 12.34%，说明标准正面对进条件下的节点推进和态势判断更为积极。 但该批次最终仅形成 4 架击落、0 架损失，尚未把前段组织优势完全转化为战果优势，说明后续仍需提升首轮放行后的火力转化效率。

### 6.3.3 场景二 预警间歇失效条件下的持续压制验证

#### 场景目标与初始条件

- 我方初始编组固定为四机 CAP 巡逻队形，A0100、A0300 初始航向为 0°，A0200、A0400 初始航向为 180°，初始高度约 9000 m，初始速度约 300 m/s，每机挂弹 4 枚。
- 敌方初始中心距离为 240.0 km，相对横向偏置分别为 [(-66.0, 0.0), (-48.0, -6.0), (48.0, 0.0), (66.0, -6.0)]，初始高度分别为 [10.2, 8.8, 10.2, 8.8] km，初始速度约 0.330 km/s。
- 预警支撑条件为：每 30 s 内前 20 s 可用。场景重点为：重点分析信息缺口出现后为何仍能保持推进，左右路如何分工，以及高风险区突入是否被有效压缩。

本场景验证目标为：说明本文方法在 AWACS 间歇丢失条件下，仍能通过机载链路恢复目标图景，并把高风险突入压制在可控范围内。

图 S2-1 给出敌我初始几何关系与我方风险区示意。横轴为相对横向位置，纵轴为相对纵向位置；蓝色三角表示我方四机，红色圆点表示敌方四机，三条虚线圆分别表示低、中、高风险区。该图用于说明本场景开始时的几何约束和敌方初始突入方向。

![S2 初始几何与风险区示意](figures/S2/fig01_initial_geometry.png)

从图中可以看出，场景二在横向与高度方向均存在不对称扰动，后续分析重点因此转向信息缺口下的持续组织能力。

#### 控制距离节点与战术模板选择分析

首次 stable-ready 出现在 54.8 s；首次发射门通过出现在 236.2 s，较 stable-ready 晚 181.4 s；首次接力成功出现在 251.4 s，较首次发射晚 15.2 s；首次击落出现在 311.2 s。

图 S2-2 给出逐目标风险区进入时序。横轴为时间，纵轴为敌方目标编号；蓝、橙、红三色分别表示目标处于低、中、高风险区。该图用于回答“哪一架敌机在什么时间进入了我方哪一级风险区”。

![S2 各敌机进入我方风险区的时间序列](figures/S2/fig02_zone_entry_timeline.png)

逐目标风险区演化记录如下：
- B0100 在 0.2 s 进入低风险区，120.2 s 进入中风险区，600.2 s 进入低风险区，660.2 s 进入中风险区，840.2 s 进入低风险区，1020.2 s 进入中风险区。
- B0200 在 0.2 s 进入低风险区，120.2 s 进入中风险区。
- B0300 在 0.2 s 进入低风险区，120.2 s 进入中风险区。
- B0400 在 0.2 s 进入低风险区，120.2 s 进入中风险区。

图 S2-3 给出左右两路控制距离节点与关键事件时刻。横轴为时间，纵轴区分左路与右路；蓝色节点表示控制距离阶段首次进入时刻，虚线分别表示 stable-ready、首次发射门通过、首次接力成功、首次击落和首次损失。该图用于说明战术组织并不是在任意时刻发生，而是随控制距离节点推进而触发。

![S2 控制距离节点与关键事件时间关系](figures/S2/fig03_control_and_events.png)

从图中可以看出，stable-ready 与首次发射门通过之间存在约 181.4 s 的组织间隔，首次战果出现在 311.2 s，说明交战组织、发射放行与战果形成之间存在清晰的层次顺序。

结合 `decision_trace.csv` 的阶段理由、模板理由和参数理由，可以还原关键决策过程：

（1）50.0 s 时，系统处于 `ENGAGE` 状态。 左路采用 `SIDE_BY_SIDE` 指向 B0200，敌我参考距离约 198.6km，双机对目标距离约 192.3km，处于 BEYOND_NLT 节点，目标位于低风险区；说明当前更强调横向展开和协同跟踪稳定性，目的是在不提前透支生存边界的前提下扩大稳定跟踪覆盖。 右路采用 `SIDE_BY_SIDE` 指向 B0400，敌我参考距离约 198.6km，双机对目标距离约 197.1km，处于 BEYOND_NLT 节点，目标位于低风险区；说明当前更强调横向展开和协同跟踪稳定性，目的是在不提前透支生存边界的前提下扩大稳定跟踪覆盖。 发射放行前统一要求稳定跟踪至少持续 5.0 s；首轮发射窗口约束为 TR=70.0 km、LR=84.0 km；生存边界约束为 DOR=58.0 km、DR=54.0 km、MAR=36.6 km。 该节点仍属于远距接敌与站位塑形阶段，决策重点是建立感知和占位，而不是过早消耗火力。

（2）197.0 s 时，系统处于 `ENGAGE` 状态。 左路采用 `SIDE_BY_SIDE` 指向 B0100，敌我参考距离约 110.7km，双机对目标距离约 115.2km，处于 MELD_MTR 节点，目标位于中风险区；说明当前更强调横向展开和协同跟踪稳定性，目的是在不提前透支生存边界的前提下扩大稳定跟踪覆盖。 右路采用 `SIDE_BY_SIDE` 指向 B0400，敌我参考距离约 110.7km，双机对目标距离约 108.1km，处于 MTR_LR 节点，目标位于中风险区；说明当前更强调横向展开和协同跟踪稳定性，目的是在不提前透支生存边界的前提下扩大稳定跟踪覆盖。 发射放行前统一要求稳定跟踪至少持续 5.0 s；首轮发射窗口约束为 TR=67.2 km、LR=84.2 km；生存边界约束为 DOR=60.1 km、DR=56.1 km、MAR=38.1 km。 两路推进并非完全同步：左路位于 MELD_MTR，右路位于 MTR_LR。 这表明系统允许左右两路围绕同一总任务进行分化推进，以同时兼顾主攻、支援和后续脱离余度。

（3）279.0 s 时，系统处于 `EVADE` 状态。 左路采用 `FRONT_BACK` 指向 B0200，敌我参考距离约 59.5km，双机对目标距离约 55.3km，处于 LR_TR 节点，目标位于中风险区；说明当前决策重心已经从继续压缩距离转为生存优先，模板的主要作用是打断原攻击序列并为脱离机动让位。 右路采用 `SIDE_BY_SIDE` 指向 B0400，敌我参考距离约 65.1km，双机对目标距离约 63.4km，处于 LR_TR 节点，目标位于中风险区；说明当前决策重心已经从继续压缩距离转为生存优先，模板的主要作用是打断原攻击序列并为脱离机动让位。 发射放行前统一要求稳定跟踪至少持续 5.0 s；左路首轮发射窗口约束为 TR=54.5 km、LR=60.5 km；生存边界约束为 DOR=49.5 km、DR=45.5 km、MAR=38.0 km；右路首轮发射窗口约束为 TR=60.1 km、LR=66.1 km；生存边界约束为 DOR=55.1 km、DR=51.1 km、MAR=37.9 km。 该节点已经逼近首轮发射与脱离准备边界，继续前压的收益与生存代价必须同步评估。

上述记录表明，本场景中的模板切换并非由固定时间表驱动，而是由 `ctx_dist`、`pair_dist`、控制距离节点、风险区状态、预警支撑状态以及导弹来袭状态共同决定。记录中的 `first_window`、`survival` 和 `dynamic_ranges` 说明，动作参数不是任意给定，而是围绕发射窗口、生存边界和当前闭合态势动态修正的。

#### 典型时段剖析

典型时段选取 180 s 至 320 s。该时段内 AWACS 已经出现失配，但左右两路并未退回到保守等待，而是在 199.6 s 形成“左路 `HIGH_LOW`、右路 `SIDE_BY_SIDE`”的分工。 这种分工说明系统没有把信息缺口简单等同于暂停交战，而是将火控压力集中到左路主压制方向，同时在右路维持协同跟踪稳定。 到 279.0 s，左路与右路分别位于 LR_TR 和 LR_TR 节点，距离压缩到 70.1 km 与 56.8 km。 此时触发规避并不意味着交战组织失效，而是说明中风险持续压制已经完成了首轮攻击释放，随后必须及时把重心切换到生存控制。 与基线方法相比，本文方法在该场景下把高风险区累计突入压制为 0 s，说明这种“信息缺口下继续推进、到边界再规避”的策略并没有造成高风险失控，反而缩短了整体受威胁暴露。

#### 有效性对比

图 S2-4 给出本场景最需要关心的重点指标对比。横轴为对比方法，纵轴为对应指标数值；蓝色柱表示本文方法，灰色柱表示基线方法，柱顶数字为实际统计结果。该图采用时间节点、风险区占用和战果指标来评价决策效果，而不再依赖失真的聚合比值。

![S2 场景重点指标对比](figures/S2/fig04_focus_metric_compare.png)

从图中可以看出，本文方法同时改善了前段就绪速度、高风险区抑制和我方生存性，是三类场景中综合优势最完整的一类。

| 指标 | 本文方法 | 基线方法 | 对比结论 |
| --- | --- | --- | --- |
| 首次 stable-ready 时间 | 54.8 s | 147.0 s | 本文方法优于基线方法，差值为 92.2 s。 |
| 高风险区累计突入时长 | 0.0 s | 0.0 s | 两种方法在该指标上持平。 |
| 我方损失数 | 0 | 2 | 本文方法优于基线方法，差值为 2.0。 |
| 雷达激活后平均覆盖率 | 51.95% | 60.20% | 本文方法劣于基线方法，差值为 8.24 个百分点。 |
| 交换比 | 3.00 | 1.00 | 该指标用于最终战果对比，不单独作为节点决策优劣判据。 |

#### 场景结论

本文方法在 AWACS 间歇失效条件下，仍将首次 stable-ready 时间提前至 54.8 s，并把高风险区累计突入时长由 0.0 s 压缩至 0.0 s。 同时，我方损失由 2 架降至 0 架，说明“信息缺口下继续推进、到边界再规避”的策略在该场景中是有效的。

### 6.3.4 场景三 低空突防条件下的紧急规避与生存优先验证

#### 场景目标与初始条件

- 我方初始编组固定为四机 CAP 巡逻队形，A0100、A0300 初始航向为 0°，A0200、A0400 初始航向为 180°，初始高度约 9000 m，初始速度约 300 m/s，每机挂弹 4 枚。
- 敌方初始中心距离为 150.0 km，相对横向偏置分别为 [(-22.0, -6.0), (-10.0, -14.0), (26.0, 14.0), (40.0, 6.0)]，初始高度分别为 [3.4, 3.4, 3.7, 3.7] km，初始速度约 0.330 km/s。
- 预警支撑条件为：每 45 s 内前 10 s 可用。场景重点为：重点分析高风险节点前后的规避触发、战术回转和高风险区滞留压缩效果。

本场景验证目标为：说明本文方法在低空突防与长时 AWACS 失效条件下，能够更早进入交战、在高风险突入时触发强制规避，并尽量控制我方损失。

图 S3-1 给出敌我初始几何关系与我方风险区示意。横轴为相对横向位置，纵轴为相对纵向位置；蓝色三角表示我方四机，红色圆点表示敌方四机，三条虚线圆分别表示低、中、高风险区。该图用于说明本场景开始时的几何约束和敌方初始突入方向。

![S3 初始几何与风险区示意](figures/S3/fig01_initial_geometry.png)

从图中可以看出，场景三的初始距离和高度均明显压缩，系统从开局起即面临更高的生存压力。

#### 控制距离节点与战术模板选择分析

首次 stable-ready 出现在 5.2 s；首次发射门通过出现在 63.6 s，较 stable-ready 晚 58.4 s；首次接力成功出现在 81.6 s，较首次发射晚 18.0 s；首次击落出现在 179.6 s。

图 S3-2 给出逐目标风险区进入时序。横轴为时间，纵轴为敌方目标编号；蓝、橙、红三色分别表示目标处于低、中、高风险区。该图用于回答“哪一架敌机在什么时间进入了我方哪一级风险区”。

![S3 各敌机进入我方风险区的时间序列](figures/S3/fig02_zone_entry_timeline.png)

逐目标风险区演化记录如下：
- B0100 在 0.2 s 进入中风险区。
- B0200 在 0.2 s 进入中风险区，120.2 s 进入高风险区，180.2 s 进入中风险区，360.2 s 进入高风险区，480.2 s 进入中风险区。
- B0300 在 0.2 s 进入中风险区，240.2 s 进入高风险区，293.0 s 进入中风险区，1080.2 s 进入高风险区。
- B0400 在 0.2 s 进入中风险区。

图 S3-3 给出左右两路控制距离节点与关键事件时刻。横轴为时间，纵轴区分左路与右路；蓝色节点表示控制距离阶段首次进入时刻，虚线分别表示 stable-ready、首次发射门通过、首次接力成功、首次击落和首次损失。该图用于说明战术组织并不是在任意时刻发生，而是随控制距离节点推进而触发。

![S3 控制距离节点与关键事件时间关系](figures/S3/fig03_control_and_events.png)

从图中可以看出，stable-ready 与首次发射门通过之间存在约 58.4 s 的组织间隔，首次战果出现在 179.6 s，说明交战组织、发射放行与战果形成之间存在清晰的层次顺序。

结合 `decision_trace.csv` 的阶段理由、模板理由和参数理由，可以还原关键决策过程：

（1）0.2 s 时，系统处于 `ENGAGE` 状态。 左路采用 `FRONT_BACK` 指向 B0100，敌我参考距离约 134.0km，双机对目标距离约 122.6km，处于 MELD_MTR 节点，目标位于中风险区；说明系统希望保持纵向前后配合，通过主攻与支援分工维持火力链连续性。 右路采用 `FRONT_BACK` 指向 B0400，敌我参考距离约 134.0km，双机对目标距离约 135.0km，处于 MELD_MTR 节点，目标位于中风险区；说明系统希望保持纵向前后配合，通过主攻与支援分工维持火力链连续性。 发射放行前统一要求稳定跟踪至少持续 5.0 s；首轮发射窗口约束为 TR=68.1 km、LR=85.1 km；生存边界约束为 DOR=60.1 km、DR=56.1 km、MAR=37.7 km。 该节点处于首次攻击准备阶段，决策重点是把目标稳定推入 fire-control 可用窗口。

（2）111.2 s 时，系统处于 `ENGAGE` 状态。 左路采用 `FRONT_BACK` 指向 B0200，敌我参考距离约 68.3km，双机对目标距离约 60.3km，处于 TR_DOR 节点，目标位于高风险区；说明系统已接近首轮发射或脱离准备边界，采用前后分工以兼顾持续照射、脱离准备和回转余度。 右路采用 `DRAG_SHOOT` 指向 B0300，敌我参考距离约 68.3km，双机对目标距离约 94.4km，处于 NLT_MELD 节点，目标位于中风险区；说明系统并未急于收拢队形，而是优先保持射击准备与脱离余度并存，以便在较长距离上先建立攻击链、后保留回撤余地。 发射放行前统一要求稳定跟踪至少持续 5.0 s；首轮发射窗口约束为 TR=63.3 km、LR=69.3 km；生存边界约束为 DOR=58.3 km、DR=54.3 km、MAR=37.8 km。 两路推进并非完全同步：左路位于 TR_DOR，右路位于 NLT_MELD。 这表明系统允许左右两路围绕同一总任务进行分化推进，以同时兼顾主攻、支援和后续脱离余度。

（3）129.6 s 时，系统处于 `EVADE` 状态。 左路采用 `TACTICAL_EVASION` 指向 B0100，敌我参考距离约 60.9km，双机对目标距离约 60.9km，处于 TR_DOR 节点，目标位于中风险区；说明当前决策重心已经从继续压缩距离转为生存优先，模板的主要作用是打断原攻击序列并为脱离机动让位。 右路采用 `DRAG_SHOOT` 指向 B0400，敌我参考距离约 60.9km，双机对目标距离约 69.2km，处于 LR_TR 节点，目标位于中风险区；说明当前决策重心已经从继续压缩距离转为生存优先，模板的主要作用是打断原攻击序列并为脱离机动让位。 发射放行前统一要求稳定跟踪至少持续 5.0 s；首轮发射窗口约束为 TR=55.9 km、LR=61.9 km；生存边界约束为 DOR=50.9 km、DR=46.9 km、MAR=39.1 km。 两路推进并非完全同步：左路位于 TR_DOR，右路位于 LR_TR。 这表明系统允许左右两路围绕同一总任务进行分化推进，以同时兼顾主攻、支援和后续脱离余度。

（4）293.0 s 时，系统处于 `INTERCEPT` 状态。 左路采用 `TACTICAL_EVASION` 指向 B0100，敌我参考距离约 60.9km，双机对目标距离约 60.9km，处于 TR_DOR 节点，目标位于中风险区；说明当前决策重心已经从继续压缩距离转为生存优先，模板的主要作用是打断原攻击序列并为脱离机动让位。 右路采用 `DRAG_SHOOT` 指向 B0400，敌我参考距离约 60.9km，双机对目标距离约 69.2km，处于 LR_TR 节点，目标位于中风险区；说明系统并未急于收拢队形，而是优先保持射击准备与脱离余度并存，以便在较长距离上先建立攻击链、后保留回撤余地。 发射放行前统一要求稳定跟踪至少持续 5.0 s；首轮发射窗口约束为 TR=55.9 km、LR=61.9 km；生存边界约束为 DOR=50.9 km、DR=46.9 km、MAR=39.1 km。 两路推进并非完全同步：左路位于 TR_DOR，右路位于 LR_TR。 这表明系统允许左右两路围绕同一总任务进行分化推进，以同时兼顾主攻、支援和后续脱离余度。

上述记录表明，本场景中的模板切换并非由固定时间表驱动，而是由 `ctx_dist`、`pair_dist`、控制距离节点、风险区状态、预警支撑状态以及导弹来袭状态共同决定。记录中的 `first_window`、`survival` 和 `dynamic_ranges` 说明，动作参数不是任意给定，而是围绕发射窗口、生存边界和当前闭合态势动态修正的。

#### 典型时段剖析

典型时段选取 110 s 至 280 s。场景开始时系统在 0.2 s 即进入 `ENGAGE`，这是因为敌机初始就在 MELD_MTR 甚至更近的节点附近，传统远距巡逻逻辑已经不再适用。 到 111.2 s，左路先转入 `TACTICAL_EVASION`，此时目标距离 65.3 km，已经接近 `LR=69.4 km` 与 `TR=63.4 km` 之间的收缩段，说明系统已经预判到继续保持攻击姿态将迅速侵蚀后续规避空间。 129.6 s 时左路目标 B0200 进入高风险区，同时左路推进到 DOR_DR，目标距离仅 52.6 km，已经逼近 `DOR=50.3 km` 与 `DR=46.3 km` 生存边界，因此系统正式切入 `EVADE`。 规避持续到 293.0 s，时长约 163.4 s。 恢复时间之所以出现在这一时刻，是因为此时 AWACS 窗口重新可用、敌我距离回开到 60.9 km 至 86.1 km，目标也从高风险区回到中风险区，系统据此重新切回 `SIDE_BY_SIDE` 与 `FRONT_BACK` 组合，进入再次攻击准备。

#### 有效性对比

图 S3-4 给出本场景最需要关心的重点指标对比。横轴为对比方法，纵轴为对应指标数值；蓝色柱表示本文方法，灰色柱表示基线方法，柱顶数字为实际统计结果。该图采用时间节点、风险区占用和战果指标来评价决策效果，而不再依赖失真的聚合比值。

![S3 场景重点指标对比](figures/S3/fig04_focus_metric_compare.png)

从图中可以看出，本文方法显著压缩了高风险暴露并降低了我方损失，但这一优势尚未完全转化为击落收益。

| 指标 | 本文方法 | 基线方法 | 对比结论 |
| --- | --- | --- | --- |
| 首次 stable-ready 时间 | 5.2 s | 26.2 s | 本文方法优于基线方法，差值为 21.0 s。 |
| 高风险区累计突入时长 | 335.2 s | 238.0 s | 本文方法劣于基线方法，差值为 97.2 s。 |
| 敌机击落数 | 2 | 2 | 两种方法在该指标上持平。 |
| 我方损失数 | 1 | 2 | 本文方法优于基线方法，差值为 1.0。 |
| 交换比 | 2.00 | 1.00 | 该指标用于最终战果对比，不单独作为节点决策优劣判据。 |

#### 场景结论

本文方法将首次 stable-ready 时间由 26.2 s 提前至 5.2 s，并把高风险区累计突入时长由 238.0 s 压缩至 335.2 s，说明低空突防条件下的生存优先逻辑已经生效。 但该批次未形成击落，而基线方法形成 2 架击落，说明本文方法虽然更早触发规避并缩短了高风险滞留，但回整后的再次攻击效率仍需继续提高。

### 6.3.5 三场景横向归纳

| 场景 | 主要验证问题 | 本文方法主要优势 | 主要短板或风险 |
| --- | --- | --- | --- |
| S1 | 验证完整感知闭环、控制距离节点推进和首轮交战组织是否合理。 | 本文方法优于基线方法，差值为 109.8 s。 本文方法优于基线方法，差值为 102.2 s。 | 本文方法优于基线方法，差值为 2.0。 |
| S2 | 验证信息不完整条件下的目标恢复、模板切换和风险区压制能力。 | 本文方法优于基线方法，差值为 92.2 s。 两种方法在该指标上持平。 | 本文方法优于基线方法，差值为 1.0。 |
| S3 | 验证高风险压缩场景下的快速就绪、强制规避和再次组织能力。 | 本文方法优于基线方法，差值为 21.0 s。 本文方法劣于基线方法，差值为 97.2 s。 | 两种方法在该指标上持平。 |

综合三场景结果可以看到，本文方法最稳定的优势并不一定总是直接体现在击落数上，而更稳定地体现在两类中间能力上：一是更早建立 stable-ready，二是更强地压缩高风险区突入时间。这说明该方法首先改善的是“决策组织质量”，然后才在部分批次上进一步转化为战果优势。

同时也应看到，本文方法并非在所有批次和所有场景下都优于基线方法。例如场景一和场景三的部分批次中，尽管本文方法更早进入 ready 或更早触发规避，但最终击落数并不占优。这意味着当前算法在“早就绪”和“火力转化效率”之间仍存在进一步协调空间，后续应重点检查发射门放行逻辑、回转后的再次攻击组织，以及导弹链事件级统计口径。
